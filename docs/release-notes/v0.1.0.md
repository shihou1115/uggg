# ugg v0.1.0 リリースノート

**ステータス**: v0.1.0 (実機検証完了 2026-06-28)
**ビルド日**: 2026-07-02
**インストーラ**: `ugg_0.1.0_x64-setup.exe` (2,779,344 bytes / NSIS currentUser モード)
**SHA-256**: `7A9356E012627EEFAF0AA29D3DE6B72F47670B78F962F43B7455F0F6E3D8F2FC`
**FileVersion / ProductVersion**: 0.1.0 / 0.1.0

## 概要

`C:\claude\ugga` での v0.0.3 プロトタイピング (2026-06-18) を起点に、仕様再定義 + 本開発を経て **M0〜M5 の機能を実装した最初のリリース**。Windows 10/11 x64 向け NSIS インストーラを `currentUser` モードで配布。

## 含まれる機能 (マイルストーン別)

### M0 起動骨格
- Tauri 2 透過ウインドウ、`%APPDATA%\ugg\` 配下にデータ配置
- ghost/shell.json パース失敗時の赤帯エラー表示

### M1 low モード対話
- 辞書 v3 (input_match / fallback / recall / monologue / events / system_messages)
- クリック透過 (8px グリッド + 50ms ポーリング)
- 起動挨拶 (時間帯別)、タイプライター (`talk_speed`)

### M2 advanced モード (LLM)
- OpenAI 互換クライアント (公式 / LM Studio / Ollama)
- コスト上限 (80% 警告 / 100% 自動降格)
- 連続 API エラーで一時降格 (5 分後復帰)
- user_profile 自動抽出 (origin=auto、上限 200 件)
- system_messages: cost_warning_80 / cost_limit_exceeded / mode_degraded / mode_recovered

### M3 存在感
- ランダムトーク (既定 10 分)、放置反応 (30 分、一度限り)、終了挨拶
- ポモドーロ (集中 25 / 休憩 5 / 4 ラウンド既定)、状態機械 + バッジ UI
- 静音モード (手動 + ポモドーロ集中時自動 + フルスクリーン自動)
- タスクトレイ + コンテキストメニュー、撫で/つつき (縦のみ部位判定)

### M4a/b voicevox_core 埋め込み + 漢字→ひらがな前処理
- 公式 C API 0.16.4 を libloading で動的ロード
- 規約同意付き自動 DL + GitHub PAT (keyring 保存)
- クレジット「VOICEVOX:話者名」常時表示
- 事前 init で初発話のラグ解消

### M4c Irodori-TTS Python サイドカー (β) — 実機 GPU 検証済
- DXGI で NVIDIA GPU 検出
- Python 3.11.9 embeddable + pip + PyTorch CUDA 12.8 (torch >= 2.10) + irodori-tts ランタイム (dacvae / silentcipher / transformers 系) + HF モデル本体 (約 4.3 GB) を設定パネルから一括 DL
- 自前 FastAPI サイドカー (`%APPDATA%\ugg\irodori\sidecar.py`)、`--download-only` / `--mock` / `--no-download` の 3 モード
- **実 Aratako/Irodori-TTS 推論結線済**: `InferenceRuntime` によるゼロショット音声クローン合成 + VoiceDesign (キャプション → 参照音声生成)。漢字混じり文は voicevox OpenJtalk の前処理でかな化してから合成
- モックモード (正弦波 wav) で GPU なし環境でも通信経路を検証可能
- アイドル 5 分で自動 shutdown、30 秒間隔のヘルスチェック、3 連続失敗で notify + 20 分の再起動抑制 (voicevox に自動フォールバック)
- `docs/quality_checklist.md` G1〜G6 を実機 (RTX 5080) で全項目 PASS (2026-06-28)

### M5 補助機能 (全 8 機能)
- M5-G: チャットログパネル (新しい順 200 件)
- M5-E: データエクスポート + 履歴クリア (JSON、`%USERPROFILE%\Downloads`)
- M5-F: ゴースト/シェル切替 UI (再起動で反映)
- M5-H: 自動起動 (`tauri-plugin-autostart`)
- M5-D: 更新通知 (update_feed_url、24h おき + 即時チェック、`update_notice_seen:<ver>` で重複防止)
- M5-C: 時事ネタ RSS (Google News + quick-xml + 暗い見出しフィルタ + advanced system prompt 注入用キャッシュ)
- M5-A: DnD ゴースト/シェル展開 (zip / フォルダ、zip slip 対策、1GB 上限、拡張子フィルタ、上書き確認 UX)
- M5-B: ツール (「N 分後」リマインダー / 時刻注入 / 📋 クリップボード補助、静音中も鳴る特例)

### UI 整理 (M6 前)
- 設定パネルを 5 ページに分割 (基本 / キャラクター / 音声 / AI・拡張 / システム)、パネル上部のプルダウンで切替

## 既知の制限 (このリリースでは対応しない)

1. **Irodori-TTS は β 扱い**: upstream (Aratako/Irodori-TTS) の main ブランチに追従する構成のため、upstream の API 変更でインストール済みランタイムとの不整合が起きうる。問題発生時は voicevox に自動フォールバックする。
2. **advanced 独り言への時事ネタ混入は将来課題**: RSS キャッシュは溜まるが、`dialogue/monologue.rs` 経路は未統合。
3. **リマインダーの自然言語抽出は「N 分後 / 時間後 / 秒後」限定**: 「明日の朝 9 時」のような曖昧表現は未対応 (将来 LLM 経由の構造化抽出で対応予定)。
4. **STT / 自動更新 / マルチ言語 / 多話者キャプション**: spec §6 将来課題のとおり、本リリース範囲外。
5. **マルチモニタの DPI 差・起動モニタ選択**: 細やかな対応は将来課題。
6. **自動更新は無し**: `update_feed_url` 経由のゴースト告知のみ。手動 DL & 再インストール前提 (コード署名証明書なし)。

## 動作要件

- Windows 10/11 x64
- 一般機能: 任意の x86_64 CPU、3 GB 以上の空き RAM
- voicevox_core (推奨): 数百 MB のディスク + 一般的な CPU
- Irodori-TTS (任意): NVIDIA GPU (CUDA 12.8 対応、VRAM 4 GB 以上)、10 GB 以上のディスク (Python ランタイム ~5 GB + HF モデル ~4.3 GB)

## アップグレード手順

新規インストールのみ。v0.0.3 プロトタイプからの DB マイグレーションは提供しない (本開発は新規 DB)。古い `%APPDATA%\ugg\` がある場合は事前にバックアップ → 削除を推奨。

## 検証ステータス

| 項目 | 状態 |
|---|---|
| `cargo check` / `cargo test` / `npx tsc --noEmit` | ✅ グリーン (94 件パス) |
| test-plan §5 手動チェック (A〜D) | ✅ 実機 OK (2026-06-22、発見したハング 6 件は修正済) |
| docs/quality_checklist.md M4c §G1〜G6 | ✅ 実機 GPU (RTX 5080) で全項目 PASS (2026-06-28) |
| インストーラ SHA-256 / FileVersion 確定 | ✅ 2026-07-02 ビルドで確定 |
| インストール版の起動検証 (本機) | ✅ 2026-07-02 サイレント再インストール → 起動 30 秒生存 + ウィンドウ表示確認 |
| クリーン Windows 環境でのインストール検証 | ☐ TBD (リリース後のフォローアップ) |

## Git タグ

`v0.1.0`

## 謝辞

- **VOICEVOX**: voicevox_core 0.16.4 (Hiroshiba 氏)、各キャラ音声モデル提供者
- **Aratako/Irodori-TTS**: Flow Matching 日本語 TTS モデル + サンプル実装
- **Tauri**: クロスプラットフォーム軽量シェル
