# ugg 要件仕様書（spec.md v1.11.6）

**フェーズ**: 本開発 Phase 1 確定版
**作成日**: 2026-06-18
**根拠**: C:\claude\ugga（プロトタイプ v0.0.3）の機能を 1 件ずつ検定した結果
**位置付け**: **要件の正本**。実装契約・モジュール構成は [architecture.md](architecture.md)（Phase 2 で作成）、テスト戦略は [test-plan.md](test-plan.md)（Phase 3）。

---

## 0. 本書の使い方

- 本書は **「何を作るか」** を定義する。「どう作るか」は architecture.md へ。
- 各機能には **★ v0.0.3 からの変更点** を明示。
- **MVP（Minimum Viable Product）** = 本書で「採用」と明記したもの全部。
- **将来課題** は §6 にまとめる（MVP には入れない）。
- 「Phase 2 で設計」と注記された項目は本書では結論まで詰めず、architecture.md で具体化する。

---

## 1. プロダクトコンセプト

### 1.1 ugg とは
「伺か」コンセプトを Tauri v2 で再構築したデスクトップマスコット。デスクトップに常駐する2体（または1体）のキャラクターが、自発的に話したりユーザーと対話したりする。

### 1.2 コアアイデンティティ
- **マスコットの存在感**: 起動挨拶・独り言・放置反応・つつき/撫で反応で「そこにいる」感を出す。
- **掛け合い対話**: メイン/サブ2体の会話表現が基本。
- **無料でも動く**: API キー無しでも辞書ベースの会話で楽しめる。
- **プライバシー優先**: 発話テキストは外部送信しない（クラウド LLM 経路を除く明示的同意済みの範囲のみ）。

### 1.3 ターゲット
- **OS**: Windows 10/11 x64 のみ
- **ハードウェア**: 通常のCPUのみで全機能動作（一部 GPU 拡張機能あり、§4.5.1 参照）

---

## 2. 採用済み技術選定（再調査しない）

| 領域 | 選定 | 理由 |
|---|---|---|
| プラットフォーム | Tauri v2 + Vanilla TypeScript + Rust | v0.0.3 実証済 |
| データストア | SQLite + keyring + ファイル資産 | 同上 |
| TTS（既定） | voicevox_core 埋め込み（libloading + プリビルドC API） | 無サーバ・CPU合成 |
| TTS（オプション） | Irodori-TTS サイドカー同梱（GPU必須） | キャプションVoiceDesignの上位品質 |
| クリック透過 | アルファマスク方式（8px グリッド + ポーリング） | 透明部分の誤クリック防止 |
| 対話モード | 二モード制（low=辞書 / advanced=LLM） | API キー無しでも動かす |
| 配布 | NSIS インストーラ（currentUser） | v0.0.3 実証済 |

---

## 3. 横断方針（全機能に適用）

### 3.1 ゴースト発話原則
**システム的なメッセージはできる限りキャラクター（ゴースト）に喋らせる**。トースト・ダイアログは補助・フォールバック扱い。

対象例:
- コスト警告（80%到達・上限超過）
- モード自動降格の通知
- 更新通知
- 自動DL完了・失敗
- 軽微なエラー

**ウインドウが見えていない間（隠している・最小化している）は出さない**（§4.6.1 と同じ「見えない間は届いていない」。v0.5.6）。
1 回だけ出す告知（コストの 80% 警告・上限到達・集計不能・アプリ更新）は、**届いたときにだけ済みにする**ので、見えていない
間に来たものは次の機会にもう一度出る。以前ここにあった「重大なものはトーストで二段表示」は実装されないまま v0.5.6 で
取り下げた（2026-09-19 ユーザー裁定。2 段目も同じウインドウの帯で、見逃しが起きる状況では 1 段目と一緒に消えるため）。

辞書 `system_messages` に対応キーを定義し、各台詞バリエーションを作者が書ける構造。発話できない状況（ゴースト未ロード・起動直後・致命的エラー）は従来のトースト/ダイアログにフォールバック。

### 3.2 「無サーバ」要件
**ユーザーが ugg を使うために、別アプリ・別サーバを手動起動する必要はない**。
- voicevox_core: アプリに埋め込み
- Irodori-TTS: サイドカーとして同梱（ユーザーから見れば「ugg の一部」）
- LLM API: 例外（クラウド/ローカルとも、ユーザーが API キー or LLM サーバを別途用意）。これはユーザーの選択で動かす対象なので「無サーバ」の対象外と整理。

### 3.3 プライバシー
- 発話テキスト・合成音声は外部送信しない。
- LLM プロバイダへの送信のみクラウドへ出る（ユーザー同意済み・既定ローカル/オフライン優先）。
- 時事ネタの検索キーワードは検索サービスへ送信（既定オフ・オンボーディング同意必須）。
- カレンダー: ユーザーが明示設定した URL への ICS 取得リクエストのみ（既定オフ。§4.6.4）。
- 天気: ユーザーが明示設定した地域の座標・地名クエリを気象 API へ送信（既定オフ・**設定行為をもって同意とみなす**・座標は市区町村粒度に丸める。§4.7.2）。
- データのエクスポート・削除をユーザーが自由にできる。

---

## 4. 機能仕様

### 4.1 キャラクター・ウインドウ

#### 4.1.1 キャラ構成（2体だがサブは任意）★
- メインキャラとサブキャラの 2 体構成を基本とする。
- 配置は伺か風: 下端揃えでタスクバーの上に立つ。**既定配置はメインが画面右端・サブがその左隣**（デフォルトシェルは main 256×384 / sub 96×144 でメイン大・サブ小）。
- **キャラはそれぞれ独立してドラッグで X 軸方向にのみ移動できる**（§4.3.4）。Y は作業領域下端（タスクバー上端）で固定。移動範囲はステージ内（＝ステージを置いているモニタの作業領域内）。**キャラをドラッグで別モニタへ移すことはできない**（ステージごと移す = 表示モニタの選択は §4.1.6）。
- **シェル/ゴーストはサブを省略可能**。サブ無しゴーストでは:
  - 画面にメインのみ表示
  - 掛け合いパターンはパターン1（main単独）相当に縮退
  - 撫で/つつきは main のみ
  - 設定 UI の「サブの声」等は無効化または非表示
- データスキーマ（shell.json）と辞書スキーマは「サブが存在しないゴースト」を表現可能にする（Phase 2）。

#### 4.1.2 キャラ画像表示（全 pose プリロード）
- 全 pose の `<img>` を data URL でプリロードし、`visible` クラス切替で表現。
- 切替時のチラつきはほぼゼロを目標。

#### 4.1.3 吹き出し描画 ★
- **吹き出しはキャラクターの左横に表示**(伺か風)。上端をキャラの顔の高さ（キャラ上端 + 高さの 12%）に合わせ、しっぽはキャラ側を向く。main/sub 両表示で重なる場合は main を上・sub を下へ退避。
- キャラが画面左端付近で左横に収まらない場合は**右横に反転表示**（しっぽも反転）。
- **タイプライター描画**（1文字ずつ表示）。
- **描画速度はユーザー設定で可変**（速い / 普通 / 遅い / 一気に表示）。
- main → sub の順に逐次表示、重ならない。
- **掛け合いパターン3/4 では 3 つ目の吹き出しを独立表示**（v0.0.3 の「同じ吹き出しに追記」方式は廃止）。
- 新発話で interrupt（古い音声・描画を打ち切る）。
  - **ただし通知は割り込まない（★v0.5.3）。** 通知は「一度出したら配達済みの記録が残り、
    二度と出ない」ので、消されると**記録だけが残って永久に届かない**。
    実際に起きていたのは、同じ周期で 2 件のカレンダー通知が配達されると後の 1 件が
    前を消すこと、および 80% コスト警告の直後にチャット応答が返ると応答が警告を消すこと。
    - 通知は**キューに入れ、描画中なら空くのを待って 1 件ずつ**出す。
    - 通知の描画中に他の発話が来たら**割り込みは許すが、通知はキューの先頭へ戻す**。
      ユーザーの入力への応答を待たせないため。戻した分は空き次第あらためて出す。
    - 対象は**通知だけ**（`notify()` 経由の system_message と、配達の Notice =
      リマインダー・カレンダー）。状況発話・独り言・定例会話（Ambient）は含めない。
      消えても記録が残らず、また出るので守る必要が無く、含めると操作を待たせるだけになる。
    - **入力促し・メニュー前口上は「ステージを使用中」として扱わない。** これらは
      いつでも出し直せる UI で、一度きりの通知とは性質が違う。ここを待ちにすると、
      入力欄を開いたまま離席したときに通知が滞留する。
- 表示継続時間はセリフ総文字数に比例。
- 全ターンの描画+発話完了後に一括消去。

#### 4.1.4 口パク（振幅駆動のみ）★
- TTS 有効時のみ動作。再生振幅（AnalyserNode RMS）でリアルタイム駆動。
- **TTS 無効時の口パクは無し**（v0.0.3 のタイマー近似を廃止）。
- pose 画像の隣に `<name>_talk.png` があれば開口フレームとして使用。無いシェルは口パクなし。
  - **`shell.json` には書かせない**（Rust 側が pose 画像の隣を自動で探す）。`poses` に混ぜると advanced の LLM に渡る pose 語彙に `normal_talk` 等が入ってしまうため、boot payload では `talk_poses` として分けて送る。
  - 駆動は TTS の本発話のみ。設定画面の音声プレビューでは動かさない（キャラが喋っているわけではないため）。

#### 4.1.5 表示スケール ★
- 設定 UI で 0.5〜2.0 の範囲で可変。
- スケールはキャラ画像のみに適用（ウインドウはステージ方式 §4.1.6 で全幅固定のため連動しない）。拡縮の基準点はキャラの足元左（bottom left）。ステージ高さは上限 2.0 でもデフォルトシェルのキャラの頭が切れないよう確保する（§4.1.6）。
- **要件: 吹き出しのテキスト・フォントサイズはスケールの影響を受けず、視認性を維持する**（v0.0.3 で課題化された問題）。
- 実装方針（Phase 2 で設計）: キャラ画像と吹き出しのレイヤーを分けて scale を別管理、または逆補正。

#### 4.1.6 ステージ（ウインドウ）とキャラ位置の永続化
- ウインドウは**モニタ作業領域の全幅 × 高さ 1024 の透明ステージ**として**作業領域下端に固定**する（＝キャラの足元が常にタスクバー上端に乗る）。高さはデフォルトシェルのキャラを表示スケール上限（§4.1.5、2.0）で拡大しても頭が切れず、その上のバルーン・入力欄まで収まる値。作業領域高さが足りなければその高さでキャップする。**ユーザーがウインドウをドラッグで動かす手段は無い**（表示モニタの切り替えのみ設定から可能。次項）。
- **表示モニタの選択（v0.4）★**: ユーザーは**設定パネルで、ステージを表示するモニタを選べる**。
  - **明示的に選択されたモニタが常に優先**される。選択が無い場合にかぎり、前回終了時の位置から推定する（`window_pos` キー。従来の挙動）。**1 秒間隔の監視は明示選択を上書きしない**。
  - 選択したモニタが見つからない場合は**主モニタへフォールバックする**が、**選択自体は保持する**。
  - **戻る保証の範囲**: OS が返すモニタ識別子は接続順で入れ替わりうるため、「同じ物理モニタに必ず戻る」ことは保証しない。**表示構成が選択時と同じであれば選んだモニタに戻る**。構成が変わっていた場合は主モニタに置き、ユーザーは選び直せる（誤ったモニタに固定されない）。
  - 選択は `monitor_pref` キーに永続化する（識別子の形式は Phase 2 設計。`window_pos`（前回位置）とは別の値）。
  - **DPI が異なるモニタへ移しても正しく表示される**（ステージ高さをそのモニタのスケールで再計算する）。
- モニタ構成・解像度・タスクバー高さ・DPI の変更は 1 秒間隔の監視で検知し、自動で再ドックする。
- **キャラごとの X 位置**（ステージ内 CSS px、視覚ボックス左端）を `char_pos` キーに永続化する。保存はドラッグ終了時（mouseup）に即時。起動時に復元し、ステージ幅に収まるよう clamp。保存が無いキャラは既定配置（§4.1.1）。**モニタを移して幅が変わった場合も新しいステージ幅に clamp する**（モニタごとに別々の X 位置は記憶しない。将来課題 §6.6）。

#### 4.1.7 タスクトレイ ★
- アイコン常駐、左クリックでウインドウ表示/非表示。
- 右クリックでメニュー（**セーフモード項目は除く**）:
  - 表示/非表示
  - モード切替（low / advanced）
  - 静音モード
  - 設定を開く
  - 終了
- メニューのチェック状態は設定変更時に同期。

#### 4.1.8 セーフモード（廃止）★
- v0.0.3 のセーフモード（不透明・枠ありで起動する保険機能）は廃止。
- ウインドウ生成は単一ラベル・単純構造に。

---

### 4.2 対話

#### 4.2.1 二モード制
- **low モード**: 辞書のみ。API キー不要・無料・オフライン。
- **advanced モード**: LLM 経由で動的生成。
- ユーザーは設定で手動切替可。
- 自動降格条件: API エラー連続 / コスト上限超過 → 一時 low に降格 → 一定時間後に復帰試行。
- **モード境界の再定義（2026-07-12、日常支援 §4.6 の採用に伴う）★**:
  - low = **決定論的・ローカル・即時・低コスト**（辞書会話、定型コマンド、リマインダー、ToDo、ポモドーロ、状況検知と定型通知、カレンダー表示 等）
  - advanced = **解釈・生成・要約・提案**（自由会話、曖昧な指示の解釈、タスク分解・優先順位提案、状況に応じた発話生成、長期記憶の参照 等）
  - **不変条件: LLM が利用不可（未設定・降格・障害）でも、生活支援機能（リマインダー・ToDo・タイマー・カレンダー表示）は常にローカルで動作する。** advanced はこれらの上に「解釈と提案」を上乗せする層であり、基盤機能の前提にしない。

#### 4.2.2 LLM プロバイダ（OpenAI 互換のみ）★
- **OpenAI 互換 API のみサポート**。
- 設定: モデル名 / `base_url`（任意） / API キー（keyring）。
- これにより以下が同じ仕組みで接続可:
  - OpenAI 公式
  - Grok（OpenAI 互換）
  - LM Studio / Ollama（ローカル LLM、OpenAI 互換エンドポイント）
  - その他 OpenAI 互換サーバ
- **Anthropic SDK 専用ルートは廃止**。

#### 4.2.2b キャラクターの人格記述（persona）★

- `ghosts/<id>/ghost.json` にゴースト作者が記述し、**advanced 経路の system prompt に載せる**。
  - `characters.main.persona` / `characters.sub.persona`: 各キャラの人格・口調・一人称。
  - `prompt.max_chars_per_line`: 1 発話あたりの目安文字数。**指示として載せるだけで強制はしない**。
  - `prompt.style_notes`: 掛け合い全体のトーン指示。
- **いずれも省略可**。省略時は役割だけの既定文（「メインキャラ。」等）にフォールバックする。
  第三者ゴーストが書いていなくても壊れない。
- 適用先は **advanced の 3 経路すべて**: チャット応答 / 独り言生成（§4.4.4）/ 定例会話の言い換え（§4.7.1）。
- **プロンプトには「設定文そのものを台詞で説明しない」旨の指示を併記する。**
  これが無いと LLM が人格記述を復唱する（「あたし、好奇心旺盛だから！」）。
- **ここに書かれたものを読むだけで、ugg が人格記述を生成することはない。**
  第三者ゴーストの辞書等から persona を機械生成して LLM に演じさせる導線は
  **§6.4 のレッドライン**で禁止している。
- 背景: v0.4.1 まで `GhostManifest` がこれらのフィールドを宣言しておらず、
  **serde が黙って捨てていた**。実測（2026-09-02、ローカル LLM・8 往復 ×2）では、
  persona 不在だと一人称の一致が main/sub とも 0/8 で、**2 人が区別のつかない同調役に潰れ、
  §4.2.4 の掛け合いが成立していなかった**。v0.5 §6.0 の項目 1 で配線。

#### 4.2.3 辞書スキーマ v3 ★
- v0.0.3 の schema v2 を整理して v3 を新規策定（**v2 からの自動マイグレーションは設けない**、作者に書き直しを求める）。
- 整理方針:
  - セクションを統合・最小化（recall_talk / update_talk 等の使い分けを見直し）
  - events キーの乱立を抑制（推奨命名・部位粒度を C-2 に合わせて簡素化）
  - 横断方針 §3.1 に対応する system 系 events を整理・追加
- スキーマ詳細は Phase 2 で策定。

#### 4.2.4 掛け合いパターン ★
- パターン1: main → sub
- パターン2: sub → main
- パターン3: main → sub → main（**3つ目の吹き出し**で表示）
- パターン4: sub → main → sub（**3つ目の吹き出し**で表示）
- **新パターン: 問いかけパターン**（極低確率発生、ユーザー応答を期待）
  - 例: 「最近どんなことに興味あるの？」「コーヒー派？紅茶派？」
  - ユーザー応答時の処理（user_profile への自動反映、follow-up 発話）は Phase 2 で設計。
  - 応答が来なかった場合のタイムアウト挙動も Phase 2 で設計。
  - **実装（v0.5.1）**: **advanced の掛け合いパターン 5** として実装。構造はパターン1
    （main → sub）と同じで、**内容がユーザーへの問いかけで終わる**点だけが違う。
    ターン数は増やさない。発生確率 5%（残り 95% は従来の重み 50/25/15/10 を保つ）。
    **サブ無しゴーストでも出せる**（main が尋ねるだけなので 2/3/4 の制約が当たらない）。
    質問は 1 つに限る（尋問にしない）。応答は次のターンで LLM が会話として自然に受けるため、
    専用の応答経路は作らない。
    - **そのために直近の会話を注入する（★v0.5.3）。** v0.5.2 まで履歴注入は常に空を返しており、
      「次のターンで自然に受ける」は**成立していなかった**（キャラが尋ねてユーザーが
      「それがいい」と答えても、その「それ」が LLM に渡らない）。v0.5.1 のリリースノートの
      説明も、この前提で書いた誤りだった。
      - **直近 8 往復を上限**に注入する（ユーザー裁定 2026-09-06）。
      - **合計文字数の上限も設け、超えたら古い方から落とす。** 往復数だけで抑えると
        長文 1 往復でプロンプトが膨らみ、コストが跳ねる。
      - **ユーザー発言の直前のキャラ発話は往復の一部として残す。** 問いかけがそこにあるので、
        上限で切り落とすと目的そのものが失われる。
      - キャラの連続発話（掛け合いの 2〜3 行）は**まとめて 1 応答**とし、誰の発言かを
        名前で残す（名前を落とすと 2 人の区別が消え、persona 配線 §4.2.2b が無駄になる）。
      - **チャット経路のみ。** 独り言の補充には渡さない（§4.4.4「独り言は応答ではない」を維持）。
      - **注入するのは advanced の会話行だけ（★v0.5.3、リリース前監査で是正）。**
        チャットログには独り言・リマインダー通知・カレンダー通知・状況発話も入る。
        これらまで送ると **§3.3 / §4.6.4 が定めた送信物の範囲を超える**
        （カレンダーで外部へ出るのは ICS 取得リクエストのみと明記してある）。
        加えて、常駐しているだけでユーザー発言が 1 件も無い窓では往復数の上限が働かず、
        文字数上限いっぱいまで独り言が入ってしまう。
        **往復が 1 つも無いなら何も注入しない。**
        問いかけパターンは advanced の掛け合いパターン 5 なのでこの絞り込みでも残る。
    - 背景: architecture が「辞書の events キー + `probability`」と書いており spec と
      矛盾していた（実装が変わるレベルの食い違い）。**spec を正本として** architecture 側を訂正した。
- サブ無しゴーストではパターン2/3/4 は使えない（自動的にパターン1へ縮退）。
- advanced のみ可変、辞書系は常にパターン1。
- 番号が小さいほど高確率（重み付け）。

#### 4.2.5 長期記憶（user_profile）★
- ユーザーの情報を保存。LLM 呼び出しの system prompt に注入。
- 設定パネルで閲覧・追加・削除。
- オンボーディングで nickname / 興味 / 話し方 / 時事ネタ ON/OFF を聞き取り → 自動投入。
- **新機能: 会話からの自動抽出**（advanced のみ）
  - LLM 呼び出し時に「記憶すべきサマリ」を副産物として抽出
  - user_profile に自動保存
  - 抽出ロジック・トリガー条件は Phase 2 で設計
- **容量管理**: 件数/文字数の上限・古いものの自動破棄/要約を Phase 2 で設計。

#### 4.2.6 会話の自動要約（記憶に統合）★
- v0.0.3 の `context_summaries` テーブルは廃止。
- 過去会話の要約は §4.2.5 の user_profile 自動抽出に統合する。
- 辞書 `recall` のトリガーも user_profile から派生。**トリガー語（`source_keywords`）はユーザーが入力した語からのみ作る（v0.5.1）**: アプリが組み立てた定型文（「ユーザーの呼び名は…」「話し方の希望: …」「興味のあること: …」）ごと渡すと、「ユーザー」「希望」「興味」といった汎用語が想起のトリガーになり、**低負荷モードの通常応答を無関係な入力まで奪う**。
  - **自動抽出（advanced）にも同じ規則を適用する（★v0.5.3）。** 記憶の本文は LLM が書いた要約文なので、そこから語を採ると LLM の言葉づかいがトリガーになる。v0.5.1 で塞いだのはオンボーディングの定型文だけで、自動抽出経路には同じ穴が残っていた。**ユーザー入力から拾った語のうち、記憶本文にも現れるものだけ**を採る。言い換えられて一致しなければトリガーを作らない（**誤爆より不発を選ぶ**。誤爆は毎回 low の応答を奪うが、不発は静かなだけ）。

#### 4.2.7 コスト管理 ★
- LLM 呼び出しのコストを `api_usage` テーブルに記録。
- 月額上限 `monthly_limit_usd` を設定。
- **80% 到達時に警告**:
  - トースト + **ゴースト発話**（横断方針 §3.1）
  - 辞書 `system_messages` に対応キーを追加（例: `cost_warning_80`）
- **上限超過時に自動降格**:
  - low モードへ降格
  - **ゴースト発話**で通知（黙って降格しない）
- 次月リセットで復帰。
  - 判定は **LLM 呼び出しの前**に行う単一のゲートとし、chat / 独り言補充 /
    定例会話の言い換えの 3 経路すべてが通る。降格タイマー（API エラー由来）で
    解除してはならない。
  - 告知済みかどうかは **当月タグを `app_settings` に永続化**して判定する
    （プロセス内フラグでは再起動で消え、月が替わっても戻らない）。
  - **集計できないときは止める（★v0.5.3、fail-closed）。** 上限が有限なら、
    当月コストを集計できない間は LLM を呼ばない。v0.5.2 まではここを通していた
    （「集計できないときに課金を止めるのは過剰」）が、v0.5.2 で**破損 DB でも
    起動を続ける**ようにしたことで集計失敗が現実の経路になり、**上限を設定して
    いるのに無制限に課金される**組み合わせが生まれた。上限が有限であることは
    ユーザーが「ここまで」と言っているということなので、守れないなら使わない。
    - **黙って low に落ちない。** 「上限に達した」とは別の告知として、
      集計できないから止めている旨をゴーストが伝える（辞書 `cost_unknown`）。
    - 告知は**プロセスごとに 1 回**。この場合の告知済みフラグは永続化しない
      （記録先の `app_settings` 自体が同時に失敗しうるため。月次の予算ではなく
      「いま壊れているか」の話なので、セッション単位で足りる）。
    - 上限 0（無制限）のときは止めない。守るべき上限が無いのに機能を落とすのは
      ただの機能低下になるため。
- ローカル LLM 利用時は記録のみ（料金は 0）。
- **当月の利用状況を設定画面（AI・拡張）に表示する**: 利用額 / 上限 / 比率。
  `get_cost_status` コマンドで取得する。
  - **料金表に無いモデルはコスト 0 で記録され、上限が発動しない。**
    ローカル LLM なら 0 が正しいので、`llm_base_url` がローカルを指していない
    場合のみ「料金を計算できないため上限は機能しない」と警告する。

#### 4.2.8 API キー管理
- keyring に保存（`service = "ugg"`, `user = provider 名` 等）。
- 設定パネルで登録（伏せ字表示）、「保存済み」状態を表示。
- 1 プロバイダ 1 キー。

---

### 4.3 ユーザー操作

#### 4.3.1 クリック種別の判別と入力導線 ★
- 最後のクリックから 250ms 待って確定:
  - 1 回 → **入力導線**（下記）
  - 2-3 回 → つつき反応
  - 4 回以上 → 連打反応
- mousedown 後の移動 4px 超でドラッグ扱い（クリック判別を抜けてキャラ移動 §4.3.4 へ）。
- **入力導線**: クリックされたキャラが「なにか用かな？」のような**入力促しの反応**（辞書 `input_prompt`、そのキャラ単独の短い発話。TTS 対象・chat_log 記録あり）を返し、**チャット入力欄をそのキャラのバルーンの上側に表示**する。
  - 促しの吹き出しは通常の表示時間で消さず、**入力欄が閉じるまで保持**する。
  - 入力欄が開いている状態での 1 クリック・Esc は入力欄と促し吹き出しを閉じる。送信時・新しい発話でも促しは置き換わる。
  - キャラ以外（吹き出し・透明部分）のクリックは main の導線として扱う。**ただしパネル・バッジ・入力欄などの UI 要素上のクリックは導線・つつきの対象外**（その UI の操作を優先する）。辞書に `input_prompt` が無いゴーストでは促し無しで入力欄だけ開く。
  - 入力欄の配置はバルーンと同じ幾何に従う（キャラ左横基準、画面左端付近では右横へ反転、キャラのドラッグに追従）。

#### 4.3.2 つつき部位判定（縦のみ）★
- 縦 3 区分: head / chest / body（**横判定は廃止**）。
- shell.json で `poke_regions.head_max` / `chest_max` を指定（横の `left_max` / `right_min` は廃止）。
- 辞書 events 探索順: `poke_<t>_<v>` → `poke_<t>`。

#### 4.3.3 撫で（ホバー往復判定）
- ボタン無しでキャラ上を往復するホバージェスチャを検知。
- 判定指標の合わせ技: 方向反転回数 ≥2 / 累積移動量 / 局所性 / 最低継続時間 / cooldown。
- 部位判定は §4.3.2 と同じ（縦のみ）。
- **パネル (設定・読み上げ・チャットログ・確認・オンボーディング) 表示中は無効**。
  パネル操作でカーソルが背後のキャラ上を往復して誤発話するため。

#### 4.3.4 ドラッグでキャラ移動（キャラ単位・X 軸のみ）★
- mousedown したキャラ（部位判定と同じヒットテスト）だけを動かす。**main / sub は独立して移動**。
- 移動は **X 軸方向のみ**。Y はステージ下端（タスクバー上端）に固定（伺かと同仕様）。
- mousedown 後 4px 超でドラッグ開始。移動範囲はステージ（モニタ作業領域）内に clamp。
- ドラッグ終了（mouseup）で位置を即時保存（§4.1.6）。
- 吹き出し表示中はキャラに追従する。
- キャラ以外（吹き出し・透明部）のドラッグでは何も動かさない。`data-tauri-drag-region` は使わない。

#### 4.3.5 右クリック → バルーンメニュー ★
- 独立したコンテキストメニュー UI は廃止し、**メニューはメインキャラのバルーン内に表示**する（伺か風）。
- **メインを右クリック**: メインが前口上（辞書 `menu_prompt.main`、例「なにか用かな？」。TTS 対象・chat_log 記録）を発話し、**続けて同じバルーン内に**メニュー項目を表示する。
- **サブを右クリック**: サブが誘導セリフ（辞書 `menu_prompt.sub`、例「用事ならメインに頼んでくれ」）を自分の吹き出しで発話した後、メイン右クリックと同じ挙動に遷移する（サブの吹き出しは表示したまま）。
- 項目（実装準拠）: モード切替（low ⇔ advanced）/ 静音モード切替 / 予定・ToDo（§4.6.1 / §4.6.2、専用パネルを開く）/ ポモドーロタイマー（§4.4.5、専用パネルを開く）/ テキスト読み上げ / 設定を開く / ウインドウを隠す / 終了（トレイの「終了」と同じ経路。§4.4.2）。ゴースト/シェル切替は設定パネルから行う。
- 閉じる: 項目実行・Esc・メニュー外クリック。新しい発話（自発発話等）でも置き換わる。メニュー表示中の左クリックは「閉じる」専用（入力導線 §4.3.1 やつつきには流さない）。
- キャラ以外の右クリックは何もしない（WebView 既定メニューの抑止のみ）。
- 辞書に `menu_prompt` が無いゴーストでは、セリフ無しでメニューのみ表示（サブ右クリックは直接メインのメニューへ）。

---

### 4.4 存在感系イベント

#### 4.4.1 起動挨拶
- 初回 = `events.first_boot`（オンボーディング兼）。
- 2 回目以降 = `events.boot`（時間帯条件 / 日付条件で分岐）。
- 静音モードでも起動挨拶は鳴らす。

#### 4.4.2 終了挨拶
- **トレイと右クリックメニューの「終了」は同じ経路**（v0.5.6。以前はメニューの「終了」があいさつも終了前の確認も無く
  即座に終わっていた）。
- **見えていれば**、今日の未完了 ToDo の確認（§4.6.2、`events.todo_quit`）か `events.quit` を再生してから exit。
- **隠している・最小化しているときは、すぐ exit**（見えない間は届いていない扱い。§4.6.1 と同じ）。隠していても音は
  鳴っていたが、そのあいさつは無くなってよい（2026-09-24 ユーザー裁定）。
- 待ち時間は台詞の文字数からの推定（最長 8.5 秒。合成の完了は待たないので、Irodori では台詞が切れうる）。待っている間に
  **もう一度「終了」を押すと、すぐ exit**。
- 強制終了（Alt+F4 でウインドウを閉じる場合を含む）では当然鳴らない。

#### 4.4.3 放置反応
- 60 秒間隔チェック、最終操作から 30 分で 1 回 idle 発火。
- 静音中・busy 中は持ち越し。
- ユーザー操作でリセット。

#### 4.4.4 ランダムトーク（独り言）★
- 一定間隔でキャラが独り言を発話。
- **既定間隔: 10 分**（v0.0.3 の既定 0=無効から変更）。
- 0 設定で無効化可能。
- advanced モードでは LLM 生成 + キャッシュ補充。
  - 発話のたびに LLM を呼ばず、**まとめて複数ストックし、キャッシュが尽きたら補充**する（LLM 呼び出し頻度を独り言の発話頻度より低く保つ）。ストックは**再起動をまたいで保持**する（起動のたびに補充が走らないこと）。
  - 補充時、時事ネタ（§4.4.6）が有効なら**その材料を一緒に渡して織り込む**（有効でなければ独り言のみ。追加の LLM 呼び出しはしない）。
  - **ストックした文にも生成時刻を持たせ、発話時に鮮度を判定する**（時事ネタを含む文の失効は §4.4.6）。
  - コスト管理は advanced 会話と同じ扱い（月額上限・自動降格に従う）。降格中・LLM 障害時は low と同じ辞書経路にフォールバックする（本節の low 側の挙動と同じ）。
  - **履歴クリア（§4.5.5）の対象**に含める。
- low モードでは辞書 `monologue` から選択。
- 静音条件下では発火しない。

#### 4.4.5 ポモドーロ ★
- 集中(work) → 休憩(break) をラウンド数だけ繰り返す。
- 既定 25 分 / 5 分 / 4 回（設定変更可）。
- 集中中は静音扱い（ランダムトーク・放置反応を止める）。一時停止中も静音扱いを維持。
- 辞書 events: `focus_start` / `focus_end` / `break_end` / `pomodoro_done`。
- **focus_end / pomodoro_done では「ねぎらいの台詞」を充実**させる（辞書側で台詞バリエーション拡充）。
- バッジ表示（画面上部中央、`.solid`）。進行状況（フェーズ・残り時間・ラウンド）を常時表示。一時停止中はその旨を示す。
- **専用 GUI パネル（`.solid`、アプリ内オーバーレイ。独立ウインドウは作らない）から操作する**:
  - **開始**: idle 時にタイマー開始。
  - **停止（一時停止 / 再開）**: 進行中のカウントダウンを一時停止し、再度押すと同じ残り時間から再開する（**新設**。従来の「レジューム無し」から変更）。
  - **中断**: タイマーを破棄して idle に戻す（従来の stop 相当）。
  - **閉じる**: パネルを閉じる。タイマーが動作中なら継続し、バッジで表示する。
  - **時間設定**: 集中/休憩/ラウンド数をパネルで編集。値は既定設定（`pomodoro_work_min` / `pomodoro_break_min` / `pomodoro_rounds`）と共有し保存される（設定パネルと同一の値）。進行中は編集不可（次の開始時に反映）。
- **右クリックメニューは「ポモドーロタイマー」1 項目**でこのパネルを開く（従来の「開始」「停止」直接項目は廃止）。トレイからの制御は現状維持。

#### 4.4.6 時事ネタ雑談
- RSS（既定 Google ニュース検索）から興味分野の見出しを取得。
- **advanced モードの独り言生成のついで**に織り込む（追加 LLM コストなし。§4.4.4 のキャッシュ補充に相乗りする）。
- **賞味期限は 1 週間**。時事ネタは鮮度が価値なので、次の 2 点で担保する:
  - **織り込み時**: 取得から 7 日を超えた見出しは材料に使わない。
  - **発話時**: 独り言のストックは補充から時間が空いて発話されうるため（§4.4.4）、**発話の時点で見出しが取得から 7 日を超えていたらその文を使わず、辞書の独り言に差し替える**。古いネタを口に出さないことを優先する。
- 暗い見出し（事故・災害等）をフィルタ。
- low モードでは時事ネタ無し。
- 取得間隔は 30 分以上にクランプ。
- 興味分野は `interest_topics` テーブル（user_profile との統合は Phase 2 検討）。
- **既定オフ**・オンボーディングで明示同意必須。

#### 4.4.7 更新通知
- `update_feed_url` の JSON を起動時に取得。
- 現バージョンより新しければ辞書 `system_messages.update_available` で**一度だけ**ゴーストが通知。
- 既定オフ（フィード URL 未設定なら無効）。

#### 4.4.8 静音モード
- `quiet_mode` 設定 ON で自発的な発話を停止。
- ユーザー操作への応答は通常通り。
- 起動挨拶・終了挨拶は静音下でも鳴る。
- トレイ・右クリックメニューから切替。

#### 4.4.9 フルスクリーンアプリ自動静音 ★
- `auto_quiet_fullscreen` 設定 ON で、フォアグラウンドウインドウがモニタ全面なら自動静音扱い。
- ゲーム・プレゼン・動画視聴中は黙る。
- Win32 API で検知、デスクトップシェル（Progman/WorkerW）は除外。
- **既定 OFF**（v0.0.3 の既定 ON から変更、ユーザーが明示的に ON）。

---

### 4.5 音声・補助

#### 4.5.1 TTS ★
- **2 エンジン体制**:
  - **voicevox_core**（既定、必須）: 公式プリビルド C API を libloading で実行時ロード。CPU合成・無サーバ。
  - **Irodori-TTS**（オプション、上級）: サイドカー同梱、GPU必須、不安定許容。
- **共通動作**:
  - 「ユーザーが別途サーバー/アプリを起動する必要はない」を両方とも満たす。
  - 失敗時は声なしで会話継続（アプリは落ちない）。
  - 全発話経路（応答・独り言・つつき・撫で・挨拶・時事ネタ・リマインダー）に乗る。
- **voicevox_core 詳細**:
  - 資産（dll / onnxruntime / Open JTalk 辞書 / .vvm）は **初回 DL** で取得
  - 規約同意必須、GitHub PAT で API レート制限緩和（任意）
  - クレジット表示「VOICEVOX:話者名」を画面下端中央に常時表示（VVM 規約準拠）
  - 事前 init（boot 時・設定変更時）で初回発話のラグを解消
- **Irodori-TTS 詳細**:
  - Python + PyTorch + CUDA + モデルをサイドカー同梱方式で配布
  - **初回 DL 方式**（軽量インストーラ → 設定で有効化時に 5〜10GB DL）
  - GPU 検出失敗時は voicevox_core にフォールバック + ユーザーに通知
  - **参照音声クローン方式**（声質の一貫性を確保）
    - 参照音声がある場合: 保存済み参照音声でクローン合成
    - 参照音声が無い場合: キャプション（VoiceDesign）から参照音声を生成 → 保存 → 以降はクローン合成へ切替
    - 参照音声の供給源は**自前生成のみ**（外部音声の取り込みは行わない＝著作権リスク回避）
    - 参照音声の再生成（キャプション変更で作り直し）・削除 UX は Phase 2 で設計
  - **漢字→ひらがな前処理**: Irodori-TTS は漢字の発話精度が不安定なため、合成前にテキストをひらがなに変換する前処理を入れる
    - 変換手段の候補: voicevox_core が同梱する Open JTalk 辞書の流用 / 形態素解析ベース（Phase 2 で選定）
    - 変換漏れ・誤読は許容（完全な読み付与は目指さない）
    - voicevox_core エンジン側ではこの前処理は不要（内部で読み解析を行うため）
  - **絵文字アノテーションの保護**: Irodori-TTS V3 は入力テキスト中の特定絵文字（45 種、upstream `EMOJI_ANNOTATIONS.md` 準拠）で感情・スタイル・効果音を制御できる。前処理はこれらの絵文字を除去せず、位置を保ったまま合成テキストに残す
    - 対応絵文字は upstream の一覧をホワイトリストとして保持（対応外の絵文字は従来通り前処理で落ちる）
    - 同一絵文字の連続（強調）も保持する
    - voicevox_core エンジン側では絵文字は解釈されないため保護しない（従来通り読み飛ばし）
  - **詳細実装は Phase 2 で**: Python ランタイム配布方式 / モデル配布 / サイドカープロセス管理 / GPU 検出 / 参照音声管理（生成・保存・再生成・削除） / **漢字→ひらがな前処理の実装方式** / 設定UI
- 設定:
  - エンジン選択（VOICEVOX / Irodori-TTS）
  - 話者選択（メイン / サブ）
  - 話速・音量
  - Irodori-TTS のキャプション入力（参照音声の初回生成・再生成に使用）
  - Irodori-TTS の参照音声管理（生成・再生成・削除）
  - 規約同意・GitHub PAT（voicevox_core 用）
- 共通の再生制御:
  - 逐次キュー（main → sub が重ならない）
  - 新発話で interrupt
    - **中断は非同期処理を跨いで効かせる（★v0.5.3）。** 合成・デコードなど待ちを
      含む処理の**あとに毎回**世代を確認し、失効していたら音も口パクも始めない。
      v0.5.2 までは合成後にしか確認しておらず、**デコード中に停止すると
      「停止したあとに喋り出す」**（その時点では再生元がまだ無いので、停止処理の
      対象にもならない）。台本読み上げ側は同じ位置で確認していたので、
      チャット発話側だけが抜けていた。
  - WebView2 自動再生ポリシー対策（`--autoplay-policy=no-user-gesture-required`）★v0.5.1 実装
    - **`additionalBrowserArgs` は wry の既定引数を「追加」ではなく「置換」する。**
      既定は `--disable-features=msWebOOUI,msPdfOOUI,msSmartScreenProtection` に
      `--autoplay-policy=no-user-gesture-required`（autoplay 既定 true のため）を足したもの。
      **自動再生ポリシー自体は既定で既に効いていた**ので、ここに自前の値を書くときは
      既定の `--disable-features=...` も必ず併記する（併記を怠ると、得るものが無いまま
      ミニメニュー・SmartScreen の抑止だけを失う）。
    - 併せて `ensureAudioCtx` で `AudioContext.state === "suspended"` なら `resume()` を
      試みる（**待たない**。待つと発話が遅れるうえ、失敗時に無音なのは変わらない）。
      これが無いと `start()` は例外も投げず Promise も解決するため、
      **正常に喋ったように見えて音だけ出ない**。

#### 4.5.2 STT（廃止）★
- v0.0.3 で実装したが MVP では搭載しない。コード・設定・コマンドを全て削除する。
- 将来必要になれば §6 将来課題から検討。

#### 4.5.3 ツール（時刻・クリップボード）
- `tools_enabled` トグルで 2 つまとめてオンオフ（個別切替なし）。
- advanced モード前提。
- **リマインダーは v0.2 で §4.6.1（統合リマインダー）へ移行済み**。`tools_enabled`・advanced から独立した常時ローカル機能で、「N 分後に教えて」などの相対時刻も §4.6.1 が扱う（静音中も鳴る特例も §4.6.1 で維持。§4.2.1 不変条件）。
- **時刻注入**: 「今何時?」「今日何曜日?」に LLM が答えるための system prompt 補強。
- **クリップボード補助**: 入力欄の📋ボタン押下時のみ読み取り、「翻訳して」「要約して」「説明して」等に応える。**📋 ボタンは `tools_enabled = false` のとき非表示**。
- 既定オフ。

#### 4.5.4 自動起動
- OS ログイン時の自動起動（`tauri-plugin-autostart`）。
- 既定 OFF、ユーザーが設定で ON。

#### 4.5.5 データエクスポート / 履歴クリア
- **エクスポートの対象（v0.5.1）**: 再生成できるキャッシュ 3 つ（`calendar_cache` / `topics_cache` / `monologue_cache`）を除く**全テーブル**。v0.4.1 まで 12 テーブル中 3 つしか出しておらず、**リマインダー・ToDo・設定が持ち出せなかった**。schema は `ugg-export-v2`。 **★v0.5.3: 部分救出**。テーブルの読み取りに失敗しても**そこで打ち切らず、読めた分を書き出す**。失敗したテーブルは名前と理由を `failed_tables` に並べ、該当テーブルの値は `null` にする（`include_profile=false` による `user_profile: null` とは意味が違うので、区別は `failed_tables` で行う）。**破損を検知したらまず export で控えるよう案内している以上、必要な場面でだけ失敗する実装は通らない。** schema は `ugg-export-v3`。
- **DB の整合性検査（v0.5.1）**: 起動時に `PRAGMA quick_check` を 1 回行う。
  - **破損していても DB は作り直さない。** 作り直すとリマインダー・ToDo・記憶・設定が一緒に消えるため。起動も止めない（起動できないよりデータを取り出せるほうがよい）。
    - **起動を止めないとは「マイグレーションが失敗しても続行する」ということ**（★v0.5.2）。破損 DB では `db_schema_version` の書き込み自体が失敗するため、ここを致命扱いにすると整合性検査を非致命に作っても意味がない（v0.5.1 は実際に setup フックが panic して起動しなかった）。**健全な DB でのマイグレーション失敗は従来どおり致命**とし、破損が検知されている場合だけ続行する。
    - **同じ理由で、整合性検査は `journal_mode` / `foreign_keys` の設定より前に行う**（★v0.5.3）。v0.5.2 まで `journal_mode=WAL` が検査より前にあり致命扱いだったため、**ヘッダやスキーマページが壊れていると検知にも保全にも起動継続にも到達しなかった**。あわせて `journal_mode` の変更は DB ファイル自体を書き換えるので、**退避を取る前に原本へ書いていた**ことにもなる。pragma の失敗は健全な DB でのみ致命とする。
  - 破損時は ① 原本のタイムスタンプ付きコピー（**`-wal` / `-shm` サイドカーも同じ規則で運ぶ**。これが無いと、本体へまだチェックポイントされていない書き込みが退避から丸ごと抜ける。実機では `-wal` が本体より大きいことがある）② `VACUUM INTO` による救出コピー（読み出せた分のみ。開いている接続を通すので WAL の内容も入る。失敗しても続行）を作り、設定 →「システム」→「データ管理」に検知内容と退避先を表示する。どう復旧するかはユーザーが決める。
  - **保全は破損 1 件につき 1 回だけ。** 既に退避があればそれを指し、作り直さない。毎起動コピーすると、破損した DB を抱えたまま使い続けるユーザー（＝この機能が対象にしている人）のディスクを起動のたびに食い潰す。先に作られた世代のほうが原本に近いので、古いものを残す。
  - **救出コピーの作成に失敗したら残骸を残さない**（★v0.5.2）。`VACUUM INTO` は失敗しても 0 バイトのファイルを残すことがあり、放置すると次回起動で「救出済み」と誤認して二度と再試行しない（「1 回だけ」のガードが失敗を成功として固定してしまう）。
  - **既存の退避・救出コピーを名前だけで採用しない**（★v0.5.3）。救出コピーは「読める DB であること」が存在意義なので、**空でないことと `quick_check` が通ることまで確認**する。原本の退避コピーは壊れた DB のバイト列コピーなので中身の健全性は要求せず、空でないことだけを見る。v0.5.2 までは名前だけで採用しており、**v0.5.1 が残した 0 バイトの `salvaged-*.db` を「救出済み」と誤認して二度と再試行しなかった**（更新経路でのみ踏む）。
  - 正常時は何も表示せず、コピーも作らない。
- **エクスポート**: 会話ログ・記憶（任意）・コスト履歴を JSON でダウンロードフォルダに書き出し。
- **履歴クリア**: 会話ログ・独り言キャッシュを削除。記憶を含めるかはオプション。
- 設定パネルから手動実行。

#### 4.5.6 ゴースト・シェル切替 ★
- **ゴースト切替時のシェル追従（v0.5.1）**: ゴーストを切り替えたとき、そのゴーストの `ghost.json` が `default_shell` を宣言していればシェルもそこへ追従する。
  - **同じ操作でユーザーがシェルも明示的に変えた場合は、ユーザーの選択を優先する**（設定画面はゴーストとシェルを同時に保存するため）。
  - 宣言が無い / 実体のシェルが存在しない場合は追従しない（切替先が無いと起動できなくなるため）。
  - 背景: v0.4.1 まで `default_shell` は出荷されているのに読まれておらず、DnD で入れたゴーストが自前シェルを連れてこなかった。
  - **追従した結果を設定フォームへ戻す（★v0.5.3）。** 保存後にフォームへ書き戻す処理がゴースト・シェルの select だけを対象外にしており、パネルを開いたまま再保存すると**追従が黙って取り消されていた**（フォームが古い id を送り返すため）。「保存済みの値をフォームへ戻す」対象からゴースト・シェルを外さない。
- 設定パネルで複数のゴースト・シェルから選択切替。
- **ホットリロード（ファイル編集の即時反映）は廃止**。作者は再起動して確認する。
- **検討: ドラッグ&ドロップ展開**
  - ゴースト/シェルの zip ファイルやフォルダをアプリにドロップ
  - 自動的に `ghosts/<id>/` or `shells/<id>/` に展開
  - 「再起動して使えるようにする」を明示提示
  - 実装可否・対応形式は Phase 2 で決定（zip 中身検証・上書き確認 UX 含む）
- **導入は非破壊で行う（★v0.5.3）**
  - **別ディレクトリで展開・検証を終えてから差し替える。** 上書き承認後に既存を先に削除してから展開すると、禁止拡張子・zip 破損・容量不足などで失敗したときに**使えていた旧版まで失われる**。悪意ある入力を必要としない、通常の更新操作の問題だった。
  - 旧版は削除ではなく**待避してから**入れ替え、入れ替えに失敗したら戻す。**戻せなかった場合は待避先を消さず、場所をログに残す**（自動で消すと、まさに守ろうとしたデータを失う）。
  - 作業ディレクトリと待避先は `ghosts/` `shells/` の**外**に置く（有効な manifest を持つディレクトリが資産一覧に現れないようにするため）。
  - **確認に使う manifest と、展開に使う manifest は同じ規則で選ぶ**（最も浅いもの。同じ深さならエントリ順で先のもの）。v0.5.2 までは確認が「最初に一致」・展開が「最も浅い」で、複数 manifest を含む zip で**確認ダイアログに出した id と実際の中身が食い違いえた**。差し替え前に展開結果の id を再確認する。

#### 4.5.7 チャットログパネル
- 簡易ログパネル（開閉式、`.solid`）。
- `get_chat_log(limit)` で新しい順 N 件取得。
- 設定パネルから開閉。

#### 4.5.8 テキスト読み上げツール（詳細: docs/text-reader-spec.md、台本形式: docs/script-reader-spec.md）
- 右クリックメニュー「テキスト読み上げ」で専用パネルを開く。パネル**表示中のみ**、
  `.txt` ファイルの DnD を受理してメインキャラの声で読み上げる（複数 DnD は先頭 1 件）。
- 入力: `.txt` のみ、1MB 上限、UTF-8 (BOM 有無) / Shift_JIS 自動判定。
- 長文対策: 文境界優先・最大 120 トークン（通常文字 1 字・Irodori 絵文字 1 個をそれぞれ 1 と数える。LLM のトークンではない）のチャンク分割（`reader_load_text` コマンド）。
  Irodori 絵文字アノテーションはどの分割経路でも分断しない。
- 合成はチャンクごとに既存 `synthesize_voice`（= 前処理・絵文字保護・フォールバックが適用）。
  再生は先読み 1 のパイプライン、失敗チャンクはスキップして件数表示。
- 読み上げ中は自発発話を抑制（`set_reading_active` コマンド → 静音判定に追加）。
  リマインダーの静音特例は従来通り。停止ボタン / パネルクローズで即停止。
- 追加コマンド: `reader_load_text(path) -> Vec<ReadingChunk>` / `set_reading_active(active)`。
  `synthesize_voice` に `caption: Option<String>` を追加（Irodori 実モデル経路のみ使用。**★v3 本体では音声に反映されない**、§6.0）。
- **台本形式対応**: `.md` 台本（`speakers` / `lines` ブロック必須 + `defaults` 任意）も
  受理する。話者 ID を ugg の slot（main / sub）へマッピングし、行ごとに話者を切り替える。
  行ごとの `speed`（再生側 playbackRate に合成）、`caption`（声質・演技指示。Irodori 実モデル
  時のみ有効、無効環境ではパネルに注記を表示して読み上げ。**★v3 本体の checkpoint は caption を条件付けに使わないため、
  現行は実モデルでも音声に反映されない**。§6.0 の v0.5.7 への引き継ぎ）、`pause_after`（行間の間）を制御。
  不正な台本はロード時に全行検証して fail-fast（部分再生しない）。詳細は docs/script-reader-spec.md 参照。

---

### 4.6 日常支援（v0.2 実装済み）★

2026-07-12 の新機能評価（評価軸と経緯は §6 冒頭）で採用した Tier S 機能群。**「ユーザーの生活状況を軽く把握し、必要なときだけキャラクターとして声をかける」**方向へ ugg を拡張する。**実装設計（DB・コマンド・モジュール・マイルストーン）は [docs/daily-support-design.md](daily-support-design.md) を正とする**（Phase 2 成果物、reviewer 反証済み）。全機能に共通する原則:

- **AI 非依存**: 基盤部分は low（ローカル・決定論）で動作する（§4.2.1 不変条件）。advanced は解釈・提案の上乗せ。
- **キャラクターが配達する**: 通知・催促はリスト UI ではなくゴースト発話が主経路（§3.1）。**通知・催促・進捗確認はサブが主体**、応答・受付はメイン（サブ無しゴーストではメイン兼務、§4.1.1 の縮退規則に従う）。
- **邪魔をしない**: すべての自発的通知は発話ガバナンス（§4.6.3）に従う。

#### 4.6.1 統合リマインダー ★
- 相対時刻（「N 分後」。v0.2 より前は §4.5.3 のツールに属していた）に加えて対応する:
  - **絶対時刻**（「18時に洗濯物」）
  - **日付・相対日**（「明日の朝ゴミ出し」）
  - **曜日指定と繰り返し**（毎日 / 毎週X曜）
  - **スヌーズ**（発火時に「あとで」を選べる。既定 10 分、Phase 2 で確定）
  - **完了 / 未完了の管理**と**通知履歴**
- 「朝 / 昼 / 夕方 / 夜」の既定時刻マッピングを持つ（設定変更可）。low の決定論パーサが解釈する語彙・書式の確定は Phase 2。
- **`tools_enabled`・advanced から独立し、常時ローカル動作**（§4.2.1 不変条件）。既存の「静音中も鳴る」特例は維持。
- 通知はゴースト発話（サブ主体）を主経路とし、発話不能時のフォールバックとして
  **アプリ内トースト**（§3.1 のフォールバック）。
  - **OS 通知（Windows トースト）は現状の実装範囲外**。フォールバックはアプリ内の帯であり、
    ウインドウが見えていなければ届かない。OS 通知の採否は §6.6 の将来課題とする。
  - したがって **ウインドウが不可視の間は「未達」として保留する**。到達扱いにすると、
    トレイに隠している間に期限が来た単発リマインダーが画面に何も出ないまま完了扱いで消える。
    保留は 10 秒ごとに再試行され、2 件以上溜まったら「『X』ほか N-1 件」に集約して 1 発話にする
    （起動時の取りこぼし回収と同じ扱い）。
  - 可視性は**到達判定**であってガバナンスではないため、`can_deliver` には段を足さない。
- advanced 上乗せ: 曖昧表現の解釈（「夕方になったら薬局」）、会話中の予定からの登録提案、通知文の生成、未完了の再通知判断。
- 登録導線はチャット入力欄の自然文（現行踏襲）。一覧・完了・削除・スヌーズの管理 UI は `.solid` パネル方式（ポモドーロパネル §4.4.5 と同型）を想定、配置は Phase 2。
- データスキーマ（既存 `reminders` テーブルの拡張 or 新テーブル）・コマンド契約は Phase 2。

#### 4.6.2 ToDo・日課管理 ★
- **期限が厳密でない作業**を管理する（厳密な時刻を持つものはリマインダー §4.6.1 の領分）。
- low:
  - 追加・完了・削除
  - 分類は **今日 / 今週 / いつか** の 3 区分のみ
  - **繰り返しタスク（日課）**: 完了しても翌日（翌週）に復活する
  - 優先度は **高 / 普通 の 2 段階のみ**
  - 未完了件数の表示（表示位置・形式は Phase 2）
  - 起動時の確認（発話ガバナンス §4.6.3 に従う）・終了前の確認（「終了」の操作への応答なので**ゲートは通さない**。
    静音中も出す — §4.4.8 の終了挨拶と同じ。見えていないときは出さずに終了する。§4.4.2）
- **キャラクター関与が本体**（一覧 UI は従属）:
  - 朝: 件数の告知（「今日は 3 件あるよ」）
  - 長時間未完了: **責めない**催促（サブ主体、頻度はガバナンス管理）
  - 完了時: 労いの反応
  - 複数日滞留: 再整理の提案
  - 対応する辞書 events キーを新設（命名・種類は Phase 2）
- advanced 上乗せ: 会話からの ToDo 抽出提案、タスク分割、優先順位・「今から何をすべきか」の提案、実行可能な量への絞り込み。
- **非採用（過剰 PM 化の防止）**: サブタスク階層・プロジェクト・タグ・工数管理。

#### 4.6.3 状況対応型自発発話と発話ガバナンス ★
- 現行の時間間隔ベースの自発発話（§4.4.3 放置反応・§4.4.4 独り言）に、**ユーザーの状況に反応する発話カテゴリ**を追加する（既存の置換ではない）。
- low の判定材料（**すべてローカルで完結**。ウインドウ内容・キー入力・スクリーン内容は見ない）:
  - 現在時刻 / PC 連続利用時間 / アイドル時間（既存基盤）
  - フルスクリーンアプリの有無（既存 §4.4.9）
  - バッテリー残量 / 音量・ミュート状態
  - ポモドーロ状態（既存）/ リマインダー・ToDo の状態（§4.6.1 / §4.6.2）
- 発話カテゴリの例: 休憩促し（連続利用）・深夜利用の声かけ・バッテリー低下・未完了フォロー。**提案 #10「PC 利用時間・休憩支援」は独立機能とせず本カテゴリに統合**。医療的判断・診断はしない。
- **発話ガバナンス（本機能の前提条件であり、後付けにしない）**:
  - **カテゴリごとのオン / オフ**（新カテゴリの既定は控えめ = 原則 OFF。既定値の確定は Phase 2）
  - **全体の最低発話間隔**
  - **夜間静音（時間帯指定）**
  - 既存の抑制系と統合: フルスクリーン自動静音（§4.4.9）・静音モード（§4.4.8）・ポモドーロ集中・読み上げ中
  - **フィードバック**: 「いまのは邪魔」をワンアクションで伝えられ、該当カテゴリの頻度が自動で下がる
- advanced 上乗せ: 直前の会話・最近の作業傾向の考慮、発話必要性の判断、同内容の繰り返し回避、キャラ別の言い回し生成。**ただし発話可否の最終ゲートは常に low 側のガバナンスが握る**（LLM 判断でガバナンスを越えて喋らせない）。

#### 4.6.4 カレンダー参照（読み取り専用）★
- **ICS（ファイル / URL）の読み込みのみ**。書き込み・双方向同期・OAuth 認証は非採用（将来課題 §6.6）。
- low:
  - ICS 取得とローカルキャッシュ（オフライン時はキャッシュを表示）
  - 今日・明日の予定表示
  - **開始 N 分前のゴースト通知**（既定 15 分、変更可）と終日予定の通知
- プライバシー（§3.3 準拠): URL はユーザーが明示設定・**既定オフ**・外部へ送るのは ICS 取得リクエストのみ。
- **ソース構成の更新は原子的に行う（★v0.5.3）。** ソースの追加・削除では設定の保存とキャッシュの全消去（`source_id` が index ベースで振り直されるため）を**両方成立させるか、どちらも起きないか**にする。v0.5.2 までは別々の文で、消去だけ失敗すると「DB は新しいソース構成 / キャッシュは旧 index / メモリと UI は旧構成」の三者がばらばらに残った。
- advanced 上乗せ: 「今日の予定は？」「次の予定まで何分？」への応答。空き時間の抽出・ToDo の予定割当提案は将来（§6.2 以降）。
- Tier A「朝・夜の定例会話」（§4.7.1）の主要材料になる前提で設計する。

---

### 4.7 定例会話と天気・生活情報（v0.3 スコープ・**実装済み** 2026-07-24）★

2026-07-18 のスコープ選定（ユーザー裁定）で Tier A（§6.2）から採用した第 2 弾。**「1 日の始まりと終わりに、キャラクターがその日の情報をまとめて話しかけてくる」同居感の完成形**。Tier S（§4.6）のデータ（予定・ToDo・未完了）と本節の天気を材料にする。実装設計（API 選定・DB・モジュール・マイルストーン）は **Phase 2 設計書（regular-talk-design.md として新設予定）**を正とする。§4.6 の横断原則（AI 非依存 / キャラクターが配達する / 邪魔をしない = 発話ガバナンス §4.6.3 準拠）は本節にもそのまま適用する。

#### 4.7.1 朝・夜の定例会話 ★
- **朝と夜の 1 日 2 枠**、それぞれ独立に有効/無効・時刻・有効曜日をユーザー設定できる（既定: **両方オフ**のオプトイン。有効化時の既定時刻は朝 8:00 / 夜 21:00、曜日は毎日）。
- **発火規則**: 「設定時刻きっかり」ではなく、**設定時刻を過ぎて最初にユーザーが PC を使っているとき**に 1 回配達する（朝 8:00 に PC が点いていない日常を許容する）。「使っている」の基準は **OS 全体の入力アイドル**（§4.6.3 の連続利用検知と同系統）が閾値未満であることとし、**連続利用中でも設定時刻経過後の最初の判定で発火する**（アイドル→アクティブの遷移待ちではない。閾値は Phase 2）。ただし**設定時刻から一定時間で当日分は失効**する（既定 6 時間・確定値は Phase 2 設計書。終日 PC を触らず夜に開いた日に朝の定例が夜間へずれ込んで配達される「夜の『おはよう』」と、朝・夜の連続配達を避ける）。1 枠 1 日 1 回。日付が変わったら未配達分は消化（翌朝に昨日の朝の分を言わない）。
- **中身（low = 定型フォーマット。材料の集約はローカルで完結）**:
  - 朝: あいさつ + 今日の予定（カレンダー §4.6.4）+ 今日の ToDo・日課の件数（§4.6.2）+ 未完了リマインダー（§4.6.1、**直近の数件に絞る** — 全件読み上げをしない。絞り方は Phase 2）+ 今日の天気（§4.7.2、有効時のみ）
  - 夜: ねぎらい + 今日の完了実績（ToDo 完了数）+ 未完了の持ち越し + 明日の予定 + 明日の天気（有効時のみ）
  - 材料が空の項目は黙って省く（空リストの読み上げをしない）。全部空でも短いあいさつは成立させる。
- **§4.6.2「朝の件数告知」との統合**: 定例会話（朝）が有効な間は、朝の ToDo 件数告知を定例会話に**吸収**し単独では発火させない（二重告知の禁止）。吸収時の 1 日 1 回管理は定例会話側で行い、**定例会話が当日配達されなかった日は件数告知も出ない**（許容する）。定例会話（朝）が無効なら従来どおり。
- **ガバナンス**: **朝・夜それぞれ独立の発話カテゴリ**として追加する（どちらも Ambient。段 3 のカテゴリ OFF が各枠のトグルに対応する）。ハード静音・夜間静音に従い、抑制中は**同日内で再試行**する。**夜間静音が優先**のため、枠の設定時刻を夜間静音帯内に置くとその枠は配達されない — 設定 UI はこの重なりを検知して警告する。🔕 フィードバックは各枠を対象とし、**3 回で当該枠のトグルを OFF にする**（1 日 1〜2 回の定例のため、Situation* の間隔バックオフ（§4.6.3）は適用しない）。
- advanced 上乗せ: 同じ材料を会話形式の言い回しに整形する（**材料の選定と発話可否は常に low が確定**し、LLM は言い回しのみ。§4.6.3 の原則）。
- 辞書 events キーを新設（朝/夜の導入・締め。命名は Phase 2）。サブ主体の原則（§4.6 冒頭）に従う。

#### 4.7.2 天気・生活情報 ★

「生活情報」は将来の傘だが、**本 v の実体は天気のみ**（それ以外を本節に足す場合は spec 改訂を伴う）。

- **キー不要・無料の公開気象 API** から予報を取得する（サービス選定は Phase 2。API キーをユーザーに要求しない）。**選定 API の利用規約に従い出典表示を行う**（§4.5.1 の VOICEVOX クレジットと同型の義務）。規約・可用性の条件を満たす API が確保できない場合は、本機能を無効のまま出荷してよい（任意の API キー方式へのフォールバックは将来判断）。
- **地域は手動設定・既定オフ**（§3.3 準拠・**設定行為をもって同意とみなす**）。地名検索から緯度経度を設定できる UI を持つ。外部へ送るのは気象 API への座標・地名クエリのみで、**送信する座標は市区町村粒度（目安: 小数 1 桁 ≒ 約 11km）に丸める**。
- low:
  - 予報の取得とローカルキャッシュ（定期 + 定例会話の直前。オフライン時はキャッシュを使い、**取得から一定時間を超えた古いキャッシュは使わず天気項目を省く** — 閾値は Phase 2、既定 6 時間目安）
  - 定例会話（§4.7.1）への材料提供: 今日/明日の天気・気温・降水確率
  - **降雨の一言**: 当日に降雨が見込まれる朝に一言知らせる状況発話カテゴリ（§4.6.3 に追加、既定オフ）。カレンダーに外出予定（当日の時刻付き予定）がある日は言い回しを強める。**朝の定例会話が有効な間は定例会話（天気項目）に吸収**し単独では発火させない（§4.6.2 の吸収と同型・二重告知の禁止）
- **本 v の非採用**（将来課題 §6.6）: 気象警報・注意報の取得、気温差アラート、週間予報の読み上げ、複数地点。
- advanced 上乗せ: 「今日の天気は？」への応答（キャッシュを会話材料に注入）。

---

## 5. 配布

- **形式**: NSIS インストーラ（`currentUser` モード、日本語ロケール）。
- **初回起動時 DL** が必要なもの:
  - voicevox_core 資産（c_api / onnxruntime / dict / models 合計数百MB）
  - Irodori-TTS（オプション、Python+PyTorch+CUDA+モデル 数GB）
- **インストーラサイズ目標**: 5〜10 MB（資産は全部初回DL）
- **インストール先**:
  - 実行ファイル: `%LOCALAPPDATA%\ugg\`
  - データ: `%APPDATA%\ugg\`（DB、TTS 資産、ログ）
    - ログは `ugg.log`（追記、2MB で `ugg.log.1` へ 1 世代退避）。パニックと、これまで `eprintln!` で stderr にしか出していなかった診断を残す。**リリース版はコンソールが無く、以前は 1 行も残らなかった。**
    - **診断ログに会話本文を残さない（★v0.5.3）。** ユーザーの入力・LLM の応答本文をログやエラー文言に載せない。**ログは履歴クリアの対象外**なので、載せると消す手段が無いまま溜まり続ける。診断に必要な情報（何が起きたか・どこで失敗したか・長さ）は本文なしで残す。
      - **対象は LLM を叩く経路すべて**（チャットの応答・独り言バッチ・HTTP エラーボディ）。
        v0.5.3 の最初の実装はチャットの応答だけを直しており、**同じ形の独り言バッチが
        漏れていた**（リリース前監査で検出）。1 箇所直して済ませない。
      - HTTP エラーボディは診断に要る（課金切れ等はここにしか出ない）ので、
        消すのではなく**長さで頭打ちにする**。プロンプトを反射するサーバがあるため、
        丸ごと載せると会話本文が落ちうる。
  - keyring: OS 標準（Windows Credential Manager）
- **更新**: 自動更新は無し、`system_messages.update_available` 経由のゴースト案内のみ（手動 DL & 再インストール）。

---

## 6. 将来課題とロードマップ（2026-07-12 再構成）

新機能提案の評価に基づく優先順位付きロードマップ。**評価軸はユーザーにとっての有用性**（利用頻度 / 代替不可能性 = OS・スマホ・既存アプリに対する限界価値 / キャラクター相乗 = キャラが配達することで価値が増すか / 負効用リスク）であり、**実装難度は評価軸にしない**。方向性: 汎用 AI アシスタント化ではなく「生活状況を軽く把握し、必要なときだけキャラクターとして声をかける常駐アシスタント」。

### 6.0 現在のスコープ: v0.5.6「速くして、乗り換えに備える」（2026-09-19 確定）★

**v3 のまま合成を速くし（約 3.6〜4.1 倍速）、v0.5.7 で依存の major 移行と新しいモデルへの乗り換えを
安全に通すための土台を、実データで確かめて出す。** 新機能ゼロ。モデルと依存の版は変えない。

v0.5.5 は差し替えを v0.5.6 へ分割していた。当初の案は差し替えまでの一括だったが、
**反証レビュー（別 Opus、2026-09-18）が「高速化は差し替え無しで届く」ことを spec 自身の実測から示した**ため、
v0.5.4・v0.5.5 と同じ形でもう一度分割する（乗り換えは v0.5.7。2026-09-19 ユーザー裁定）。
前回の記録は `docs/release-notes/v0.5.5.md`。

### 反証レビューと検証で分かったこと（2026-09-18、実コードで確認）

**1. 高速化は差し替えの人質ではなかった。**
v3 の sway 8 は、参照音声の事前変換（参照 wav を latent へ 1 回だけ変換して使い回す。音はビット同一）まで
入れると 213 / 260 / 294 ms で、同じ条件の MF（bf16）の 202 / 235 / 246 ms と**ほぼ同じ速さ**（下の実測表）。
v3 の sway 8 の音は 2026-09-14 にユーザーが許容と裁定済み。どちらも `sidecar.py` の中で完結し、
`sidecar.py` は**毎起動でリソースから置き換わる**ので、数 GB の更新なしに全員へ届く。
当初案の「一括でなければ高速化が届かない」は成り立たない。
（**反証の後の訂正**: 当初この節は、事前変換ありで測った sway 8 の値を、ugg のいまの経路 — 合成のたびに
参照 wav を変換する（約 35 ms）— の速さとして書いていた。事前変換なしなら約 3.2〜3.5 倍にとどまり、
参照 wav のままの MF の約 1.2〜1.3 倍の時間がかかる（どちらも段ごとの内訳から出した推定。壁時計の比較では
35 ms の差が揺れに埋もれている）。**事前変換を項目 1 に含める**ことにした — 2026-09-19 ユーザー裁定）

**2. 更新の途中で失敗すると、成功済みの退避を戻さずに消す。**
`update_irodori_runtime` は、どの段で失敗しても（モデル取得・名前付き要件・固定 URL の退避・固定 URL の
入れ直しの 4 か所）退避ディレクトリを丸ごと消して返る（固定 URL の段では、失敗したものを戻せた場合に限る。
戻せなければ退避を丸ごと残して返る）。**それより前に入れ直しに成功した固定 URL のパッケージの退避
（＝唯一残った旧版）も一緒に消える**。
名前付き要件にはそもそも退避も復元も無い（コード自身が自認）。対象はアルファベット順に 1 本ずつ
処理されるので、`huggingface_hub` が `transformers` より先に上がる。
「失敗しても動いていた環境を壊さない」は、v0.5.7 の主経路で破れる。

**3. どのモデルを読むかは、ビルドの定数だけで決まる。**
`model_args()` は起動（読み先）と `--download-only`（取得先）の**両方**に同じ定数を渡している。
このまま v0.5.7 で定数を新モデルに変えると、**アプリを入れた瞬間から、数 GB の更新を終えるまで
高品質モードが使えない**（重みが無く `FileNotFoundError` → VOICEVOX）。そのときキャラが言うのは辞書の
「GPU 環境が整っていないようだ」で、**事実と違う案内**になる。しかも記録の欄が空の環境の読み先が
決まっていない。受け皿の固定値（`V054_BASELINE_MODELS`）は更新の判定では使っているのに、
読み先の決定には使っていない（v0.5.4 の実機検証・v0.5.5 の監査に続く 3 回目の同じ形）。

**4. 合成を 1 回試す経路がどこにも無い。**
v0.5.5 の §6.0 が「major を上げる前に必ず入れる」とした条件の受け皿が無い。更新中は錠
（`IrodoriBusyGuard`）のせいで通常の経路からはサイドカーを起動できない。

**5. 新版のインストーラは、動いている ugg を強制終了してから入る。**
生成された NSIS スクリプト（`src-tauri/target/release/nsis/x64/`、v0.5.5 のビルド。tauri の版に依存する）を
読んだ結果。ugg が動いていると確認を出し、OK なら終了フックを通らない形で止める。直前 5 分以内に
高品質音声を使っていれば、サイドカーが GPU を掴んだまま残る。残ったものは次の起動の掃除が止めるが、
掃除は誰にも待たれていないので、新版の最初の合成と重なりうる（項目 3e と同じ穴）。
何も手を打たなければ、乗り換えの回でも残った孤児と新しいサイドカーが VRAM を取り合い、空きによっては
合成が入らない（項目 4 の Job Object で塞ぐ）。

### 項目（この順序）

**1. 速くする（v3 のまま）**

- 通常合成を linear 40 ステップ → **sway 8 ステップ**へ（`sidecar.py` の `num_steps` と
  `t_schedule_mode`。`sway_coeff` は計測と同じ -1.0 のまま）。依存もモデルも触らない
- **参照音声を事前に変換して使い回す**（2026-09-19 ユーザー裁定）。いまは合成のたびに参照 wav を
  変換している（約 35 ms）。参照 wav・合成モデルとコーデック（repo@revision）・両者の精度・参照の前処理の
  組ごとに 1 回だけ変換し、以後はその結果を渡す（どれかが違えば別に変換する。値を作るのはコーデックなので、
  コーデックだけを更新したときも作り直す。参照を作り直すとファイル名が変わるので自然に作り直される。
  revision が `main` の間は、上流が中身を差し替えても名前は変わらない — 固定の revision へ上げるまでの限界）。
  **上流の公開 API は変換結果をファイルのパスでしか受け取らない**（`SamplingRequest.ref_latent` はパスの
  文字列で、中で読み込む）ので、変換結果はファイルに置く。置き場所は参照 wav の隣
  （`%APPDATA%\ugg\irodori\refs\` の下）で、参照音声を消す・作り直すときに一緒に消す。
  **変換結果を作る公開 API は無い**ので、ランタイム自身が合成のたびに使っている非公開のメソッド
  （`_load_reference_latent`）を 1 回だけ呼ぶ（2026-09-14 の計測と同じ方法で、値はランタイムの内部と同じ）。
  **作れない・読めないときは今までどおり参照 wav を渡す** — 上流が変わってこのメソッドが無くなっても、
  遅くなるだけで喋れる。変換結果を使った合成が失敗したら参照 wav でやり直し、**それで合成できたときだけ**
  変換結果を消して、そのサイドカーが止まるまで使わない（壊れている・合わないなら、作り直しても毎回失敗する）。
  参照 wav でも失敗したときは本文などの問題なので消さない。GPU のメモリ不足は変換結果のせいではないので、
  作る段でも使う段でも止めず、やり直さない。（2026-09-19 のスコープ確定の時点では「サイドカーのメモリの
  中だけ」、次に「公開 API のまま」と説明していたが、上流のコードを確かめて改めた。）
  音がビット同一なのは 2026-09-14 の実測（linear 40・中くらいの文 1 本、ファイル経由）。変換結果は
  サンプラーより前の段なので、ステップ数を変えてもサンプラーに入る参照の値は同じ
- **参照音声の生成（VoiceDesign、`no_ref`）は据え置く。** 通常合成と同じ `_make_request` を共有しているが、
  sway 8 を測ったのは参照音声つきの合成だけで、生成は一度きりなので速さより品質が効く。
  **用途 2 つの値を分けるだけで、設定の仕組みは作らない**（開発方針 2）
- この値は v0.5.7 でも捨てない。乗り換え後も、更新が済むまでと失敗して戻ったときは v3 で喋るので、
  **v3 用の値として残る**
- 実測は約 3.6〜4.1 倍速（868 / 946 / 1087 → 213 / 260 / 294 ms、下の表）。音は 2026-09-14 にユーザーが
  自分の参照音声で聴いて許容
- 高速化の実施はユーザー裁定（2026-09-14・09-19）。既存モデルの合成パラメータの変更なので、§6.5 の凍結
  （新しい音声モデル・演技機能の追加）には当たらない
- 確かめ方: 合成 1 回の所要時間とステップ数を `ugg.log` に 1 行残す（いまは出ていない。本文と caption は
  残さない — §3.3・v0.5.3 項目 7）。インストール版でその値を見る／ユーザーが聴いて許容か
- 改訂: architecture §2.4 の資産の表（`refs\` の下に変換結果のファイルが増える）、参照音声の削除
  （変換結果も消す）

**2. 落ちた理由が読める（日本語も、進捗も）**

- **stderr の文字化け（cp932）は、原因を確かめてから直す。** → **2026-09-19 に確かめた。** サイドカーの
  起動時に `sys.stderr.encoding`・`locale.getpreferredencoding(False)`・`sys.flags.utf8_mode` などを 1 行
  ログへ出すようにし、ユーザーの端末で起動した dev（v0.5.5 の記録では dev でも化けていた）のサイドカーの中で
  観測した。結果は `stderr.encoding=cp932`・`locale=cp932`・`utf8_mode=0`・`isolated=1` で、日本語の見本は
  ugg.log で化けた。**原因は CPython の既定** — パイプへ書くとき、UTF-8 モードでなければ ANSI コードページで
  書く。同梱の Python は `._pth` で isolated なので、環境変数（`PYTHONIOENCODING` / `PYTHONUTF8`）では
  変えられない。**直し方: `sidecar.py` の冒頭で stdout / stderr を UTF-8 に切り替える**（`reconfigure`）。
  `-X utf8` は `open()` の既定の文字コードまで変え、モデル側のコードに影響しうるので使わない。
  観測の 1 行は切り替える前と後の文字コードを並べて残し、インストール版でも効いているかを実機検証で確かめる。
  v0.5.5 の手元の再現が UTF-8 だった理由（`-S` 付きで、Claude のツールの中＝MSIX のコンテナから起動）は
  特定していない
- **UTF-8 決め打ちの読み取りは 3 か所ある**（サイドカーの stderr / `run_python` ＝ pip とモデル取得の出力 /
  VOICEVOX のダウンローダ）。`reader.rs` に既にある「UTF-8 で読めなければ Shift_JIS」を流用して
  一度に閉じる（原因によらず効く受け側の守り）。**pip の「アクセスが拒否されました」は、v0.5.7 で
  いちばん読みたいログ**
- **進捗を行ごとに流す。** `run_python` は子プロセスが終わってから出力をまとめて流すので、数 GB の取得中は
  画面が 1 行のまま固まる（v0.5.5 の項目 3 が約束した「進捗を初回 DL と同じ水準で」は、基準の初回 DL の
  側から成立していなかった）。区切りは `\n` と `\r` の両方、文字コードの判定は行が確定してから。
  **`run_python` の呼び出し 13 か所を 1 件ずつ**、stdout と stderr が混ざって壊れないか（マーカー行の解析・
  最後の 1 行の保持）を判定する（開発方針 7）。同期待ちが非同期のワーカーを塞ぐ件（v0.5.4 で見送り）も
  同じ改修で閉じる。 → **2026-09-20 に実装した。** 子プロセスの起動と読み取りを `tts::child_process` に
  切り出し、stdout と stderr を別々に読んで**行が確定してから**文字コードを判定する（`\n` と `\r` の
  両方で区切り、`\r` だけで終わった行と、カーソルを上へ戻す並びを含む行は「上書き」として扱う。
  色付けの制御文字は落とす）。**13 か所のうち、混ざって壊れるのは import の確認 1 か所だけ**だった
  （理由に「最後の 1 行」を使う）ので、そこは **stderr の**最後の行から取る形にした。ほかは画面へ流すだけで、
  マーカー行と版の行は 1 行ごとに解釈するので順番に依存しない。**パイプ越しでは pip も huggingface_hub も
  取得中に進捗を出さない**ことが分かった（pip の rich は端末でないと描かず、hub の tqdm は `disable=None` の
  既定で端末でないと出ない）ので、pip には `--progress-bar raw`（pip 24.1 以降の選択肢。入っている版を
  見てから付ける）を渡して「取得中 N / M MB（P%）」に直し、`sidecar.py` はモデル取得の間だけ hub の判定を
  「自動なら出す」に差し替える（実際の hub 0.36.2 で、差し替えの前は出ず後は出ることを確かめた）。
  同期待ちは `block_in_place` でワーカーの持ち分を渡してから待つ（zip の展開も同じ）。
  **VOICEVOX のダウンローダも同じ形**（終わってからまとめて読む）だったので一緒に直した — こちらは
  色付けの制御文字を落とすときに 1 バイトずつ文字に積み直しており、**日本語のエラー文が化けていた**
  （`ff68e6c` のコミットメッセージと architecture がこの経路も読めると書いていたのは誤り。同じレビューで発覚）
- **無進捗のタイムアウトと中断を付ける。** いまは待つだけで、固まると再起動まで Irodori が使えない
  （更新の錠をコマンド全体で握っている）。 → **2026-09-20 に実装した。** 数えるのは「出力も、読み書きも、
  CPU も無い」時間で、5 分で止める。**出力だけで数えてはいけない** — pip は torch を展開している間、
  黙って書き続けるだけで CPU もほとんど使わない（Windows の CPU 時間は約 15.6ms 刻みで課金される）。
  読み書きと CPU は Job Object の集計（孫まで含む）で見る。止めるときは **Job ごと**止める
  （pip が起こしたビルドのプロセスまで。子だけ止めると孫がパイプを握って読み取りが終わらない）。
  **「中断」は、固まったものを自動で止めることとして実装した** — ユーザーが押す中断ボタンは新しい
  コマンドになり、この項目が挙げている害（固まると再起動まで使えない）は自動で止めれば解ける。
  あわせて、**錠を握ったまま上限なく待つ通信**（Python 本体と get-pip.py の取得）に接続 30 秒・
  読み取り 60 秒の上限を付けた（同じ形の取り残し。ダウンローダ自身の取得と、更新の確認も同じにした）
- **stderr の経路にも伏字を通す。** 伏字はいま HTTP のエラー応答の本文にしか掛かっていない。v0.5.7 は、
  例外文やログの出方がわからない新しいランタイムの stderr を全部ログへ流すことになる（spec §3.3・
  v0.5.3 項目 7「診断ログに会話本文を残さない」）。 → **2026-09-20 に実装した。** 送る前に本文と
  キャプションを覚え（直近 8 回分。stderr は応答より先にも、要求が終わったあとにも届く）、ログと画面に
  出す前に伏せる。**そのままの形では一致しないことがある**ので、行ごとの断片と、Python が `repr` で
  書き換える文字（全角空白 U+3000、絵文字の結合 U+200D。溜息の絵文字に入る）で区切った断片も伏せる。
  同じ穴が 500 の本文の側にもあったので一緒に直した。**いまのランタイムには本文を stderr に書く経路は
  見つかっていない**（合成は `log_fn=None` で呼び、例外文にも本文は入らない）ので、これは v0.5.7 の
  新しいランタイム向けの守り。照合で伏せることの限界（正規化や独自のエスケープ）は下の
  「見つけたが今回の裁定に含めていないもの」の白名簿化のまま
- 改訂: architecture §15 のリスク表（文字コードの行・Job Object の行）、§1 のモジュール表
  （子プロセスの出力の読み方を共有する ＝ `child_process.rs`）、§8.3（取得の進捗の出し方）、
  test-plan の E-10（進捗・無進捗・伏字の確認）

**3. 更新を 1 つのトランザクションにする**

`update_irodori_runtime` の作り直し。下の a〜f は同じループと末尾を触るので 1 項目に束ねる。
実装は a（記録の意味）→ b（ゲートの設計）→ c〜f の順。 → **実装では a → d → c → b → e → f の順にした**
（2026-09-23）。b の不合格は「全部戻す」で終わるので、d より先に作ると戻せないのに「戻す」と言う期間ができる。

- **a. 読み先を記録から決める。** 決定表: 記録に値あり → それ／欄が空・記録なし →
  **`V054_BASELINE_MODELS`**（固定値。要件やモデルを変えてもこの定数は変えない）／導入していない →
  いまのビルドの値。**起動の経路から必ず通る純関数**に置く（いまの基準値の書き足しは、設定パネルを
  開いた人にしか走らない）。`model_args()` を**読み先（記録）と取得先（いまのビルド）に分け**、
  「取得先と読み先に同じ値を渡す」という今の不変条件を書いたコメント 3 か所（`model_args()`・サイドカーの
  起動・`--download-only` の呼び出し）を、新しい不変条件へ書き換える。
  `sidecar.py` の既定値は **v3 のまま据え置く**（Rust は常に 3 つとも渡すので既定値が使われるのは
  引数を渡さない経路だけだが、そのときは重みのある旧モデルを読む側に倒す）
- **b. 成否は「1 回合成できる」で見る。** 更新中は錠で通常の経路を使えないので、`sidecar.py` に
  **一発で合成するモード**を足し、更新処理の子プロセスとして確かめる（HTTP の経路と錠は開けない）。
  決めること:
  - 材料の参照音声（既存の wav を使う / 固定の wav を同梱する — 同梱なら `tauri.conf.json` の
    resources への追加が付く）
  - 通す経路（通常合成と参照音声の生成の 2 つ）
  - **VRAM 不足は「移行の失敗」と区別して伝える。ただし既定は全部戻して「VRAM を空けてから
    もう一度」と案内する**（安全側）。戻さずに保留すると、新しい依存の上で記録上の旧モデルを読む
    ＝ 誰も確かめていない組み合わせが残る。v0.5.7 の spike で v3 が transformers 5 で動くと分かれば、
    保留を許すかを再判断する。v0.5.4 の実機検証②（ゲートの誤判定で既存環境では必ず巻き戻る）の
    再発は、VRAM 不足を失敗と区別して案内することで避ける。ローカル LLM が 7.7 GB 常駐する環境では普通に起きる
  - 時間切れ、GPU が無いときの扱い
- **c. 段取りを固定順にする。** ① 名前付き要件を **1 回の pip** で ② 固定 URL の 3 本 ③ モデル取得
  ④ 合成で確かめる → 記録。`outdated` の並び（アルファベット順）に依存させない。依存の連鎖で
  PyPI の **CPU 版 torch** が入らないよう守る（いまは torch を名指ししたときだけ CUDA の取得元を使う）。
  `sidecar.py` のモデル取得に渡している `local_dir_use_symlinks=False`（huggingface_hub 1.x で削除済みの引数）は
  **引数ごと削る** — v0.5.7 には、新しい `sidecar.py` が**更新前の依存のまま**動く期間がある
- **d. 途中で失敗したら、入れ替えたものを全部戻す。** 名前付き要件にも退避を作る（pip が入れ替える配布を
  事前に把握して退避する、など手段は着手時に設計する）。**戻せないものが残るなら、確認ダイアログと
  取説にそう書く**（v0.5.5 で、実装に無い「戻す」を約束しかけた前例がある）
- **e. 更新の直前に孤児を確実に止める。** 孤児の掃除は起動時に投げたまま誰も待たず、更新は自分の
  サイドカーしか止めない。更新の入口で掃除を待つ。`request_shutdown` は応答の中身と、TCP が閉じた
  ことまで確かめる（いまは送れたかどうかだけで、4xx / 5xx も成功扱い。doc コメントも実装と食い違っている）
- **f. 導入・更新の錠をプロセスをまたぐものにする**（2026-09-19 ユーザー裁定）。いまの錠
  （`IrodoriBusyGuard`）はプロセスの中だけで効く。同じ錠を見ているのは 3 経路（更新・初回導入・合成の側の
  サイドカー起動の判定）で、2 つの ugg が同時に動くと、退避と復元を互いに壊す（しかも更新の入口は、
  残っている退避を先に書き戻す）か、片方の更新中にもう片方が入れ替え途中の site-packages でサイドカーを
  起動する。3 経路とも同じ錠に入れ、**プロセスが落ちたら自動で外れる形**（OS のファイルロック）にする —
  ファイルがあるかどうかで判定すると、落ちたときに永久に更新できなくなる。
  **もう片方の ugg が既に動かしているサイドカーは止めない**（所有者が生きている — 項目 4）。そのサイドカーが
  掴んだファイルで入れ替えが失敗したら d で全部戻し、「もう 1 つの ugg を終了してからもう一度」と案内する。
  画面に出る挙動は変えない（完全な single-instance は入れない — 下の「入れないもの」）
- **実データで通す。** 分割の弱点は、依存の major 移行を実際に通さないと、ゲートが本番で効くかを
  確かめきれないこと。v0.5.6 の実機検証で、**途中の失敗からの全戻し**と**合成ゲートの不合格**を、
  実データで少なくとも 1 回ずつ通す。手段は出荷物の要件・モデルを変えないものに限る — ユーザーの端末で、
  試験の前に `installed.json` を控え、**名前付き要件と固定 URL の欄だけ**を古い値へ書き換えて更新の対象を作り、
  試験の後に控えへ戻す。`models` の欄は、手元に重みがある値にしか変えない（a で記録が読み先になるので、
  重みの無い値にすると高品質モードが止まる）。v0.5.5 の E-8 は記録を退避する手だった（今回は欄を書き換える）。
  手順は test-plan に足す。あわせて**インストール版で合成ゲートが合格すること**も確かめる
  （同梱物の漏れは dev では分からない）
- 改訂: architecture §2.4 の `installed.json`（記録が読み先の正本になる）、契約表の
  `update_irodori_runtime`・`download_irodori_assets`・`get_irodori_status`（ゲート・退避・段取り・排他）、
  f の錠のファイル（§2.4 の表と §8.1 のツリー）、`sidecar.py` の一発合成モード。
  `irodori_download.rs` のコメント「1 回合成できるまでの検証は v0.5.6（major 移行）で入れる」はこの実装で書き換える
- **実装した形（2026-09-20〜23、`572305c` / `8cf60a1` / `a72b51f` / `d04075f`）。** 設計案を反証にかけ
  （4 観点のうち「動いている環境を壊す経路」の 1 観点だけが完走し、残りは週の利用制限で落ちた）、指摘 14 件を
  全件採った。上の a〜f から変えた・足したところ:
  - **a**: 決定表は**名前ごと**に引く（`models` は名前ごとに欠けうるので、記録全体を単位にすると「1 本だけ欠けた
    記録」の読み先が未定義になる）。「導入していない → いまのビルド」の行は、起動の経路が手前で止まるので置かない。
    記録の書き込みは tmp → rename（書きかけを読むと読み先が基準値へ倒れる）。起動時に「読み先をどこから決めたか」を
    `ugg.log` に 1 行残す（実データの確認の観測点）。**v0.5.6 では基準値といまのビルドが同じ値**なので、決定表は本番で
    一度も分岐しない（初めて分岐するのは v0.5.7）— 実データの確認で、記録を手元に重みがある別の値へ書き換えて分岐を通す
  - **b**: 材料は**いまある参照音声の写し**（2026-09-20 ユーザー裁定。無ければ確かめずに成立させて記録に残す）。
    **GPU は差分で見る** — 「見えなければ飛ばす」だけだと、更新が CUDA を壊したときも成功として記録が進み、更新
    ボタンが消えて戻す導線ごと失われる。前は使えたのに使えなくなったら戻す。元から見えない環境だけ飛ばす
    （2026-09-20 ユーザー裁定）。**合否を絶対値で決めない** — 不合格なら全部戻し、戻した状態でもう一度試して「更新が
    原因」か「元から合成できない環境（更新のせいではない）」かを言い分ける（合成は共有の HF キャッシュの掃除などでも
    落ちる）。VRAM 不足は別の案内で戻す（例外にならずプロセスごと落ちたときは、始めたときの空き VRAM が 4 GB 未満なら
    VRAM 不足とみなす）。締め切りは 10 分（読み込み時間の実測が無いので、合格したときの所要時間を ugg.log に残して集める）。
    通すのは通常合成だけ（参照音声の生成は別モデルを 1 つ余分に読み、VRAM の狭い環境ほど更新できなくなる）
  - **c**: 名前付き要件は **torch 系を先に CUDA の index から**、それ以外を 1 回の pip で（1 回にまとめると `--index-url`
    が全体に効く）。それ以外を入れるときは **torch をいまの版に縛る**（依存の解決で PyPI の CPU 版を取りに行かせない）。
    それでも CUDA 版が CPU 版へ入れ替わったら失敗にする
  - **d**: 控えは**全配布の版**（`importlib.metadata` の全走査。記録用の関数は名指しの約 24 件しか見ず、pip が連鎖して
    入れ替える依存が入らない）。失敗したら、固定 URL は退避から、名前付きの配布は**控えの版を `--no-deps` でまとめて**
    入れ直す（torch 系は別の pip。固定 URL の 3 本は PyPI に無いので外す）。**名前付きの配布を戻すには通信が要る**ので、
    戻せなかったものは控えを残し、次の更新の前にもう一度戻す（確認ダイアログと取説にそう書いた）。モデルは戻さない
  - **e**: **生きているサイドカーが 1 件でもあれば始めない**（設計案の「site-packages は掴まない」は誤り — 生きている
    サイドカーは torch の DLL を読み込んだままで、Windows は消すことも上書きすることも拒む。入れ替えも全戻しも失敗する）。
    入口での**掃除（止める）は項目 4 の所有者の欄が入るまで行わない**（応答の形だけで止めると、もう 1 つの ugg の稼働中の
    サイドカーを止めうる）。入口でやるのは、起動時の掃除を待つことと、記録を確かめる（読むだけ）こと。
    **初回導入も同じ入口を通す**（自分のサイドカーを止めていなかった）
  - **f**: `LockFileEx`（ファイルのバイト範囲の錠）。共有モードで締め出す方式はウイルス対策や検索インデクサが開いた
    だけで誤判定し、名前付きミューテックスは `await` でスレッドを移るコマンドと噛み合わない。錠はプロセスの中の印と
    二段（一本にすると同じ ugg の 2 本目とも衝突する）。合成の側は `adopt_sidecar` でも見る（入口だけだと、2026-09-14 の
    監査が塞いだ「起動の途中で更新が始まる」穴が、他プロセスの更新で開く）
  - **自動テストで届かないもの**（実物の Python・torch・モデルが要る）: pip の順序と torch の縛りが効くこと、控えの版への
    入れ直し、ゲートの合格・不合格・VRAM 不足の実際の出方。実データの確認（test-plan E-10 の 4）で通す

**4. 孤児と二重起動**

- **台帳の記録に所有者（ugg の pid）を持たせる。** 2 つ目の ugg の孤児掃除が、1 つ目の稼働中の
  サイドカーを応答の形で「自分のもの」と見て止めうる（直前まで使っていた場合、1 つ目はヘルス監視で
  20 分止まり、キャラが「使えません」と告知する）。自分以外の、生きている ugg の記録は止めない（生きているかは pid だけでなく開始時刻でも確かめる —
  pid は再利用されるので、pid だけだと別のプロセスが同じ pid を取ったときに孤児が永久に残る）。
  **所有者の欄が無い記録（v0.5.5 が書いたもの）は、所有者がもう生きていないものとして従来どおり
  掃除する。** 欄を足しても台帳全体の読み込みが失敗しないこと（いまは 1 件でも読めないと台帳が
  丸ごと空になる）を、v0.5.5 の書式の台帳を使ったテストで固定する
- **台帳の読み書きの錠もプロセスをまたぐものにする。** いまの錠はプロセスの中だけで効き、読んでから書き戻す
  作りなので、2 つの ugg が同時に書くと片方の記録が消える（v0.5.5 で直した形が、プロセスをまたいで再発する）。
  3f と同じ種類のファイルロックに入れる
- **Job Object**（閉じたら中身ごと終わらせる設定）で、強制終了・Alt+F4・異常終了のときも子プロセスが
  一緒に終わるようにする。**6 か所のうち 2 か所（`run_python` と VOICEVOX のダウンローダ）は、
  項目 2 の無進捗の中断で先に入れた**（止めるときに孫まで止める必要があり、読み書きと CPU の集計も
  Job から取る）。残りはサイドカーと zip の展開。**効くのはこの版を入れた後から**（インストーラが止めるのは Job Object を
  持たない旧版）＝ **v0.5.7 を入れる瞬間の v0.5.6 を守る**。子プロセスを起動する本番のコードは 6 か所
  （サイドカー / `run_python` / zip 展開の powershell / VOICEVOX のダウンローダ / 取説を開く notepad と
  ShellExecuteW）。取説を開く 2 つはユーザーの窓なので割り当てない（ugg と一緒に閉じてはいけない）。
  そのため **ugg 自身は Job に入れず、対象の子を 1 つずつ入れる**（ugg 自身を入れると、あとから起動する
  子が所属を引き継ぎ、取説の窓まで一緒に閉じる。子が起こす孫は引き継ぎで入る）。
  テストの中の ping と taskkill は対象外
- **実モデルの ON/OFF が、サイドカーの稼働中は効かない**のを直す（起動済みのポートを、mock を見ずに、
  錠の判定より先に返している）。数行の修正で、**v0.5.7 の実機検証（mock と実モデルの往復）の前提**。
  v0.5.4 の実機検証で v0.5.5 へ計上した（改訂履歴 v1.8.6）が、v0.5.5 の §6.0 の引き継ぎから落ちていた
- 改訂: architecture §2.4 の `sidecars.json`（`[{port, pid}]` に所有者が加わる）と台帳の錠のファイル、
  §15 のリスク表の Job Object の行
- **実装した形（2026-09-24）。** 上から変えた・足したところ:
  - **持ち主**: 記録に `owner: {pid, started}`（`started` はプロセスの開始時刻）。判定は「この ugg / 生きているほかの ugg /
    持ち主なし」の 3 つで、掃除が止めるのは持ち主なしだけ（この ugg の記録にも触らない）。v0.5.5 の記録（欄なし）と
    旧 `ready.json` だけの手がかりは持ち主なし。持ち主のプロセスを開けないときも終わっているとみなす。
    **この版が書いた台帳は v0.5.5 も読める**（持ち主なしの記録には欄を書かない。v0.5.5 は知らない欄を無視する）
  - **台帳の読み書き**: 1 件ずつ読む（読めない記録があっても残りを失わない）。書き込みは一時ファイル → 差し替え
    （上書きの途中を別の ugg が読むと、壊れた JSON を「台帳なし」と読んで全部消す）
  - **錠**: `sidecars.lock`（3f と同じ `LockFileEx`）とプロセスの中の錠の二段。**足すときは錠が取れなくても書き、消すときは
    見送る**（足し損ねると強制終了で孤児を追えない。錠なしで消すと、ほかの ugg が足した記録を消しうる。消し損ねた
    記録は次の掃除で片付く）。待つのは 2 秒まで
  - **Job Object**: ugg が生きている間だけ中身を生かす Job を 1 つ持ち（ハンドルは ugg が終わるまで閉じない）、サイドカーと
    zip の展開を起動の直後に入れる。**子プロセスを起動する本番のコードは 7 か所になっていた**（項目 3b の 1 回合成の
    ゲートが増えた）。ゲートは `run_python` と同じく実行ごとの Job の中で走るので対象外。取説の 2 つは入れない
  - **実モデルの ON/OFF**: ハンドルに起動したモードを持たせ、違えば止めて起動し直す。導入・更新の錠の判定を、既存の
    ポートを返すより先にした（更新中は、動いているものがあっても新しい依頼を VOICEVOX へ流す。走っている発話は切らない）
  - **入口の掃除**（項目 3e の続き）: 導入・更新の入口で、起動時の掃除を待ったあと、**持ち主のいない孤児を止めてから**
    生きているものを数える。断るときの説明は持ち主ごとに分けた（ほかの ugg → そちらを終了 / 止められなかった孤児 →
    PC を再起動 / この ugg のもの → ugg を再起動）
  - **自動テストで届かないもの**: 本物のサイドカー（python・torch）が ugg の強制終了で一緒に終わって VRAM が戻ること、
    実モデルで ON/OFF の切り替えが効くこと。実データの確認（test-plan E-10 の 5）で通す。ugg の強制終了で結びつけた子が
    終わること自体は、試験の実行ファイルを「ugg」役にして強制終了する自動テストで確かめた

**5. 終了のあいさつを揃える**（2026-09-19 ユーザー裁定）

- 「見えていれば、今日の未完了 ToDo の確認（§4.6.2）かあいさつ（§4.4.2）をしてから終了。隠していれば
  すぐ終了」の 1 本にまとめ、トレイと吹き出しの右クリックメニューの両方から通す
- これで 2 つの穴が同時に消える: メニューから終了する人には §4.6.2 の終了前の確認が一度も出ない／
  隠している最中にトレイから終了すると、何も表示されないまま最長 8.5 秒止まる（トレイ側は
  ウインドウが見えているかを見ていない）
- 気を付けること:
  - 待ち時間は文字数からの推定で、合成の完了を待っていない（Irodori では台詞が切れうる）。
    待っている間にもう一度「終了」を押せる
  - あいさつの発話が実モデルのサイドカーを起動し、その直後の終了で孤児になりうる（項目 4 の Job Object で塞ぐ）
  - 隠したウインドウでも音が鳴るなら、「隠していればすぐ終了」で聞こえていたあいさつが無くなる。
    §4.6.1 と同じく「見えない間は届いていない」扱いに揃える判断で、実機で音の有無を確かめる
- 改訂: §4.4.2・§4.3.5・§4.6.2（終了前の確認は、隠しているときは出さない。発話ガバナンスを通るかも
  ここで揃える — 実装のトレイの経路は通していない）、architecture の終了経路（§14 ほか）、取説。
  `lifecycle.rs` のコメント「揃えるかは v0.5.6 への引き継ぎで判断する」はこの実装で書き換える
- **実装した形（2026-09-24）。** ユーザー裁定（2026-09-24）: **隠していればすぐ終了し、聞こえていたあいさつが無くなってもよい**
  （実機で音の有無を確かめる手順は要らなくなった）。
  - 終了の経路を `commands::lifecycle::quit_with_farewell` の 1 本にし、トレイと右クリックメニュー（`quit_app`）の両方から
    通す。「見えている」は §4.6.1 の配達と同じ判定（最小化も見えていない扱い）
  - **待っている間にもう一度「終了」を押すと、すぐ終了する**（上の「もう一度押せる」の扱い。以前はトレイの「終了」を
    2 回押すと、あいさつが 2 回流れた）
  - **発話ガバナンスは通さない**と揃えた（§4.6.2 の「ガバナンスに従う」は起動時の確認だけに掛け、終了前の確認は
    「終了」の操作への応答として扱う。実装のトレイの経路がそうなっていた。静音中も鳴る §4.4.8 と揃う）
  - 右クリックメニューの「終了」でも、終了前にウインドウの位置を保存するようになった（以前は保存していなかった）
  - **あいさつを待つ間は自発発話を止める**（2026-09-24 の実機確認で発覚: 待っている間に advanced の独り言が流れ、
    あいさつを置き換えた。トレイの経路にも以前からあった）。自発発話の単一経路（`deliver_event`）が取る busy の permit を
    終了の側が握る。取れないとき（いま喋っている最中）は待たずに進む（終了を止めない）。ポモドーロの区切りの発話と
    システムの告知（`notify`）はこの経路を通らないので止まらない（どちらも終了の数秒に重なることはまれ）
  - 掃討: 終了する場所（`app.exit(`）はこの 1 か所だけ。Alt+F4 でウインドウを閉じる経路は「終了」ではなく強制終了の
    扱い（あいさつは無く、サイドカーは項目 4 の Job Object で一緒に終わる）

**6. 告知は「届いた」と確かめてから済みにする**（2026-09-19 ユーザー裁定: §3.1 の二段表示は取り下げ）

- **§3.1「重大なものはトーストで二段表示」は取り下げる。** 2 段目のトーストは同じウインドウの中の帯なので、
  見逃しが起きる状況（隠している・最小化している）では 1 段目と一緒に消え、届く先が増えない。
  §4.6.1 は同じ事実から「見えない間は未達として保留する」と既に決めており、OS 通知は §6.6 の将来課題。
  割り込みで告知が消える問題は v0.5.3 の通知キューで解決済み
- **代わりに実害のある穴を直す。** `notify()` はウインドウが見えているかを見ずに発話を出し、結果も返さない。
  そして呼び出し元の 4 か所（コストの 80% 警告と上限到達・コスト集計不能・アプリ更新の告知）が、
  **届いたかを見ずに「告知済み」を記録する**（うち 3 か所は発話より前に記録する）。隠している間に出ると、**コストの 2 件はその月、集計不能はその起動のあいだ、アプリ更新はその版では
  二度と出ない**。モードの降格は画面に出す場所が無く、発話が唯一の伝達手段。§4.6.1 の「見えない間は
  未達として保留し、見えたら出す」を `notify()` に移し、届いたか保留したかを返す（または届いたときに
  済みにする）形にして、4 か所の呼び出し元を直す（新しいコマンド・イベントは要らない）。
  Irodori が使えないときの告知も 5 分の間隔を発話の前に刻むが、次の失敗でまた出るので対象外とする
- 改訂: §3.1、`notify()` と呼び出し元 4 か所、architecture §11.1 / §11.2（severity の素案。告知の種類の一覧と既定の表も実装から外れているので
  一緒に書き直す）、`notify.rs` のコメント。§4.6.1 と daily-support-design §3.2 の「§3.1 の二段構え」は、
  発話できないときにトーストへ落とすフォールバック（§3.1 の末段落）を指しており**取り下げの対象ではない**。
  紛らわしいので「§3.1 のフォールバック」に改める

- **実装した形（2026-09-24）。**
  - `notify()` は見えていなければ出さずに「保留」を返し、出したら「届いた」を返す（出せなければ「失敗」）。
    **保留した告知を後から自動で出す仕組みは持たない**（新しいコマンド・イベントを足さない）。1 回だけ出す告知は
    済みにしないので、次の機会（次のコスト判定・次の更新確認）にもう一度出る
  - 1 回だけ出す告知の 4 か所は、同じ補助（`once_reached`: 済んでいなければ出し、届いたときだけ済みにする）を通す。
    上限到達は、届いたときだけ続けて降格の告知も出す
  - それ以外の告知（資産 DL の完了・失敗、Irodori が使えない、降格・復帰）は、見えていない間は出さずに終わる
    （DL の結果は設定パネルにも出る。Irodori は次の失敗でまた出る。降格・復帰はチャットの応答の中で起きるので、
    ふつうは見えている）
  - 掃討: ほかの「1 回だけ」の記録（深夜の声かけ・バッテリー・朝の ToDo・カレンダー）は `deliver_event` を通り、
    もとから届いたとき（か辞書に無いとき）だけ済みにしている。チャットの turn で上限到達・集計不能を返答として出す
    経路は、見えている（ユーザーが入力した）ので対象外

各項目の確かめ方（自動テスト／実機）は実装時に test-plan へ足す。項目 5・6 は、単機能のテストでは
捕まらない機能どうしのつなぎ目の型（v0.5.3）なので、操作列テスト（test-plan §3.2b）の対象にする。

### 実装上の制約

- **v0.5.6 では要件もモデルも版を変えない**（項目 1 は合成パラメータの値と、サイドカーの中の事前変換だけ）。
  依存の major 移行とモデルの差し替えは v0.5.7
- **共有 HF キャッシュ（`~/.cache/huggingface/hub`）を掃除の対象にしない**（据え置き）。
  他アプリの資産が同居している。ugg が消してよいのは `%APPDATA%\ugg\irodori\` の下だけ
- **実機検証はユーザーの端末で行う**（CLAUDE.md の規律。Claude デスクトップのツールから dev・
  インストーラ・更新処理・DB を開く処理を実行しない）

### v0.5.6 に入れないもの

- **完全な二重起動ガード（single-instance）** — 2026-09-19 ユーザー裁定。台帳の所有者（項目 4）と更新の錠
  （項目 3f）だけ入れる。2 つ目を起動させない挙動は spec に無い機能の追加になる。
  入れないので残るもの: 2 つの ugg がそれぞれリマインダーを配る（二重配達）。2 つが同じ DB へ同時に
  書くときの待ち（busy_timeout）の扱いは未確認
- **§3.1 の二段トースト** — 2026-09-19 ユーザー裁定で取り下げ（項目 6）
- **ダウンロード資産のハッシュ検証** — v0.4.1 で Medium と判定して見送ったものを、今回も継続して見送る
  （HF は etag で照合する。GitHub のアーカイブと Embeddable Python は素の https）
- **CUDA Graph + バケット化** — 2026-09-14 の記録どおり、乗り換えの後に再判断する（下の調査結果）
- **`phasefield-audio/Irodori-TTS-v4.1-Anime`** — **不採用（2026-09-11 裁定、据え置き）**。
  学習データの出所を確認できないものを出荷物の既定に据えない。base（v4.1-Small）自身の
  倫理条項の第 1 項が「**声優**・著名人・公人の声を本人の明示的な同意なくクローン・
  なりすましに使わない」であり、その条項との関係を検証できない。§6.4 のレッドライン、
  Live2D・伺かゴースト変換の不採用と**同じ基準**を適用する
- 量子化版（int8 / int4 / fp8）— v0.5.5 の「入れないもの」から継続
- v0.6 のデフォルトゴースト・シェル刷新と E-6b の実機検証（引き続き v0.6）

### v0.5.7 へ引き継ぐもの（乗り換えの回）

**v0.5.7 は MF への乗り換え**: transformers 4→5（`>=5.12.1,<6`）/ huggingface_hub 0.27→1.x /
irodori-tts の pin を上流 `89f9d8fb` 以降へ / 合成モデルを `Aratako/Irodori-TTS-v4.1-Small-MF`（bf16・4 ステップ）へ。
v0.5.6 の土台に乗せて届ける。

**差し替えに付随する作業**（2026-09-18 の検証で分かったもの）:

- 合成パラメータをモデルごとに持つ。**MF は実行時に渡したステップ数・サンプラーの種類・CFG を無視して
  4 ステップで回る**（2026-09-14 の計測で、ランタイム自身が info として出力した）。渡しても無視される値を
  渡し続けないよう、モデルごとに渡す値を分ける（v3 用の値は v0.5.6 の項目 1 のもの）。ugg の値を渡したときに
  例外や音の崩れが出ないかは spike で確かめる
- 上流の pin を `89f9d8fb` 以降へ上げたとき、参照音声の変換結果の受け取り方（`ref_latent` がファイルのパス）が
  変わっていないか確かめる（v0.5.6 の項目 1 がこれに乗る）
- **トークナイザの取得。** モデルの取得は `model.safetensors` の 1 ファイルだけなので、「チェックポイントの隣に
  置けばローカルで解決」（下の調査結果）が、ugg の取得経路ではそのままでは成り立たない。取れたかどうかの
  判定も要る（`assets_ready` はモデルを見ていない）
- `model_precision` を bf16 に（`_build_runtime` は参照音声の生成も通る。VoiceDesign を残すなら v2 は fp32 のまま）
- `huggingface_hub` を指定しているのは共通の要件の `==0.27.0` の 1 か所だが、Irodori の段で `transformers` を
  `--upgrade` すると連鎖で上がる（実機は 0.36.2）。上げるときは、連鎖した結果と記録が一致するように指定する。
  固定の基準値（`V054_BASELINE_REQUIREMENTS`）は変えない（変えると、v0.5.5 の監査で直した「更新が永久に
  届かない」が再発する）
- 取得量の数字が 3 か所で食い違っている（確認ダイアログ 約 2〜3 GB / 進捗表示 約 2〜4 GB / 取説 合計 10 GB 前後）
- 「更新があります」を設定パネル以外でも知らせる（いまは設定パネルを開いた人にしか見えない）。
  失敗の説明を事実に合わせる（いまは理由によらず「GPU 環境が整っていない」と言う）。VRAM が足りないときに
  伝える経路（v0.5.5 の §6.0 が「要る」とした）。**どれも告知の追加（`NoticeKind` と辞書キー）で spec の改訂を伴う**
- 旧モデルの容量（v3 + VoiceDesign + MF で数 GB が残り、消す導線も空き容量の確認も無い）と、要求から
  消えた名前（pin・要件・モデル）が記録に残り続ける件（`outdated_pins` は現在の要求の側しか見ない。
  pip uninstall を実行する経路は 1 つも無い）

**裁定が要るもの**:

- **§6.5「高品質 TTS の新音声モデル・演技機能の追加は凍結」の扱い。** 差し替え・caption の有効化・
  絵文字アノテーションは「演技機能」に当たりうる。文言の整理ではなく裁定として決める
- **参照音声の生成（VoiceDesign）。** §4.5.1 の要件で、ugg で参照音声を作る手段はこれしか無い
  （外部音声の取り込みは §4.5.1 が禁止）。MF が `no_ref` + caption で作れるなら畳む。作れなければ
  v2-VoiceDesign を残すか、§4.5.1 を改訂する。`MODEL_PINS` から名前を消しても、`sidecar.py` の
  既定値と取得対象の一覧が旧モデルを使い続ける
- **既存ユーザーの声が変わる**ことを無言で通すか、参照音声の作り直しを案内するか
  （§4.5.1 の参照音声クローン方式の目的は声質の一貫性）
- **透かし（SilentCipher）** — 有効化（`pydub` を足す）か、pin ごと外すか。v0.5.5 の §6.0 は v0.5.6 での裁定を
  約束していたが、**v0.5.7 で決めることを 2026-09-19 にユーザーが裁定した**（2 回目の先送り）。理由: v0.5.6 は
  要件の版を変えない（土台の回で失敗の原因を切り分けやすくする）／効いたかを確かめる合成ゲートが v0.5.6 で
  初めて入る。「外す」は pip uninstall の経路を新しく作ることになる
- **倫理条項の提示**（DL と更新の確認ダイアログ、取説、`credit.ts` の「帰属表示義務がない」という断定の
  4 か所）。提示だけにするか、同意を記録するか（記録は設定の追加で spec の改訂を伴う）

**受け入れ条件に入れるもの**:

- **caption が効くこと。** test-plan の S4 / S4' と script-reader-spec §5.2 の S4 / S4' の期待値（sidecar ログの
  `SamplingRequest.caption`）は、そのようなログが無く、caption は伏字の対象なので原理的に満たせない。
  「**seed を固定し**、同じ文・同じ参照音声で caption なしとありを合成して差が出る」へ書き換える。対照として、
  同じ seed・同じ caption の 2 回がビット同一であることも確かめる（いまのサイドカーは seed を固定しておらず、
  固定しないと caption が効いていなくても毎回違う音になって条件が必ず通る。比較のときだけ seed を固定する
  手段が要る）
- **絵文字アノテーション 45 種**（§4.5.1、`preprocess.rs`、取説に一覧）は **v3 のモデルカードが出典**。
  MF が同じ意味で解釈するかは未確認。caption と同じ形の乖離を新しく作らないため、受け入れ条件に入れる

**着手前の spike**（前回・前々回と同じ扱い。計測用の取得物は scratchpad に置き、共有 HF キャッシュを
使わず、用が済んだら消す — 2026-09-14 裁定 C）:

| 確かめること | 合格条件 | 判定 |
|---|---|---|
| v3 が transformers 5 で動くか | v3 の重み + transformers 5 で 1 回合成できる | エージェント |
| MF が `no_ref` + caption で参照音声を作れるか（あわせて MF の `use_caption_condition` の値を記録する） | wav が出る → その声がキャラに使えるか | エージェント → ユーザー |
| MF が ugg の固定 codec（`Aratako/Semantic-DACVAE-Japanese-32dim`）で合成できるか（09-14 の計測は上流の既定の codec） | 1 回合成できる → 音が崩れない | エージェント → ユーザー |
| MF が 45 種の絵文字を解釈するか | seed を固定して、絵文字のあり・なしで音が変わる（対照: 同じ seed・同じ文ならビット同一） | エージェント → ユーザー（聴く） |
| MF に ugg の合成パラメータ（`cfg_scale_*`・`num_steps` など）を渡したときの挙動（ランタイムは無視すると出力する） | 例外が出ない → 音が崩れない | エージェント → ユーザー |

MF のリポジトリの構成（`model.safetensors` 3.09 GB・`tokenizer/`・テキストエンコーダの重みは同梱）は
2026-09-14 の計測で分かっている。確かめるためだけに取り直さない。

### 引き継ぐ調査結果（再調査しない）

- **v4.1-Small は VoiceDesign を畳んだ構成。** `MODEL_REPO_VOICE_DESIGN` と
  `_load_voice_design` の作り直しを伴う。加えて**短い単一参照での類似度低下**が
  ugg の作りに正面から当たる（`VOICE_REF_READING_TEXT` は 1 文、`max_ref_seconds` は
  30.0 固定）。**既存ユーザーの参照音声を作り直す導線**が要るかを差し替え時に判断する。
- **トークナイザは合成時にダウンロードされる。** v4.1 が要求する
  `sbintuitions/modernbert-ja-310m` は共有キャッシュに**無い**。差し替え後の初回発話が
  数百 MB の無言 DL になり、120 秒のタイムアウトを越えれば 500 → 無言フォールバック。
  オフラインでは永久に失敗する。**「同意と進捗」がいちばん効く場所で破れる**ので、
  トークナイザも取得対象に含めるかを差し替えの設計で決めること（MF については下の注記）。
- **v4.1 の漢字読み改善は ugg には効かない。** ugg は OpenJTalk でかな化してから渡す。
  CER の出典も ugg が使わない `600M-v3-VoiceDesign` の値である。
- **`caption` は一度も効いていない。** v3 本体が `use_caption_condition: false` なのに、
  契約表と script-reader-spec は使えると宣言している。v4.1-Small は `true` なので
  差し替えで閉じるが、**有効化されること自体を検証対象**にする（MF の値は記録していない。上の spike で確かめる）。
- **倫理条項の提示。** v4.1-Anime を外した根拠が採用モデル側の条項なのに、
  ランタイム DL の確認文言は VOICEVOX のものだけで Irodori の条項に触れていない。
  差し替えと同じ回で扱わないと、乖離をもう 1 件作る。
- **`model_precision` は `fp32` 固定。** VRAM 削減の余地が未検討のまま残っている。
  → **2026-09-14 実測: bf16 は速くならない**（v3・MF とも）。VRAM のピークだけが減る
  （v3 3.5 → 2.6 GB、MF 4.6 → 3.1 GB。torch の確保量）。
- **高速化（2026-09-14 ユーザー裁定。2026-09-19 に v0.5.6 = v3 の sway 8 + 参照の事前変換 / v0.5.7 = MF と分けた）。**
  実測（RTX 5080・llama-server 常駐・短 / 中 / 長の 3 文の中央値）:

  | 構成 | 生成時間 | 備考 |
  |---|---|---|
  | v3 現行（参照 wav・linear 40） | 868 / 946 / 1087 ms | 拡散ループが約 9 割。参照の準備 約 35 ms |
  | v3・参照 latent 事前計算・linear 40 | ほぼ同じ | 参照の準備 35 → 0.7 ms、**音はビット同一** |
  | v3・参照 latent 事前計算・linear 16 | 399 / 444 / 488 ms | 約 2.2 倍速 |
  | v3・参照 latent 事前計算・sway 8 | 213 / 260 / 294 ms | 約 3.6〜4.1 倍速（**v0.5.6 で入れる構成**）。参照 wav のままなら約 +35 ms（段ごとの内訳からの推定） |
  | `Aratako/Irodori-TTS-v4.1-Small-MF`（4 ステップ、fp32、参照 wav） | 212 / 243 / 325 ms | VRAM ピーク 4.6 GB |
  | MF（bf16、参照 wav） | 213 / 246 / 257 ms | VRAM ピーク 3.1 GB |
  | MF（bf16、参照 latent 事前計算） | 202 / 235 / 246 ms | v0.5.7 の構成の見込み |

  - **音はユーザーが聴いて v3 の linear 16・sway 8 とも許容**（ユーザーの参照音声で判断）。
    **MF も bf16 で「十分許容範囲」**（2026-09-14 ユーザー裁定。fp32 と bf16 を聴き比べたうえで）。
    差し替え先を MF（bf16）にする判断材料として扱う。
  - **MF は CFG を蒸留に焼き込んだ 4 ステップ**（1 ステップ 1 回の forward。実行時の CFG とサンプラーの設定は
    無視される — ランタイムの info）。モデルカードの話者類似度は
    RF 40 ステップに近い（0.743 / 0.752）。上流 `89f9d8fb` 以降・`transformers>=5.12.1,<6` が要る。
    **テキストエンコーダの重みはチェックポイント同梱**で `sbintuitions/modernbert-ja-310m` の重みは取らず、
    トークナイザはリポジトリ同梱の `tokenizer/` を**チェックポイントの隣に置けばローカルで解決**される
    （前項の「合成時にダウンロード」は、隣に置く限り MF では起きない。ただし ugg の取得経路は
    `model.safetensors` しか取らないので、そのままでは隣に置かれない — 上の「差し替えに付随する作業」）。
  - **同じ条件（参照 latent 事前計算）なら、速さは MF と v3 の sway 8 でほぼ同じ**。MF の利点は
    「少ステップでの品質」側にある。内訳は拡散 120〜150 ms・長さ予測 40〜55 ms（v3 は 15 ms）・デコード。
    （表の v3 の行のうち linear 16 と sway 8 は、当初「参照 latent 事前計算」の条件を書き落としていた。
    2026-09-19 に計測の記録と突き合わせて直した）
  - **記事の CUDA Graph + バケット化は見送り**（再判断は差し替え後）。v3 は 1 ステップ約 21 ms で、
    記事が CUDA Graph を当てた後の値（18.6 ms）に近く、効き目は記事ほど出ない見込み。ランタイム内部への
    差し込みが大きく、差し替えで作り直しになる。
  - 計測に使った取得物（モデル・transformers 5 系・上流ソース）は計測後に削除した。共有 HF キャッシュは使っていない。

### 見つけたが今回の裁定に含めていないもの（次のスコープ選定で扱う）

2026-09-18 の検証で見つけた。v0.5.6 の主題と関係が薄い、またはユーザーの裁定を取っていないので、
扱うかは改めて決める。

- **`export_data` の部分救出が画面に出ない。** 読めなかったテーブルはファイル内の `failed_tables` にしか
  載らず、画面は「保存しました」だけを出す。DB 破損のときに案内している控えの手段なので、
  会話ログだけ落ちた控えを「全部控えた」と信じて原本を捨てる経路が残る
- **カレンダーの背景取得の失敗はログにしか残らない。** ICS の URL が失効すると、カレンダーの通知が
  静かに 1 件も来なくなる
- **サイドカーの接続が切れるたびに、asyncio のエラー（6 行）がログに出る**（2026-09-19 の dev の確認で発見）。
  合成 6 回のうち 2 回、`_ProactorBasePipeTransport._call_connection_lost` の `ConnectionResetError` が出た。
  v0.5.5 以前から出ていたもので（化けて読めていなかった）、合成は成功している。ugg 側が接続を閉じたときに
  Windows の asyncio（Proactor）が出すものと見られる（推測）。害は無さそうだが ugg.log を埋める
- **伏字の白名簿化**（ログに残す形を例外の型名や WinError 番号などに限る）— 一致に頼る伏字の限界を
  原理的に塞ぐが、診断の能力とのトレードオフになる。stderr にも伏字を通す側は項目 2 で入れる
- **実機検証の残り**: v0.5.5 の E-8 の任意 3 件（名前の記録が無いので、E-8 の 16 項目と突き合わせて特定する）／
  v0.5.3 の実機未確認 3 件（`export_data` の部分救出・連続通知・履歴注入）。v0.5.6 の実機検証の回で
  まとめて消化するかを決める

### 配布まわりを載せない裁定（据え置き）

実ユーザーが開発者本人のみのため、コード署名・自動更新・配布の体裁は扱わない。
**未署名のまま公開している**ことは既知の事項として記録しておく。


### 6.1 Tier S — v0.2 リリース済み（2026-07-18、タグ v0.2.0）

**§4.6「日常支援」として要件化・実装済み**: ① 統合リマインダー ② ToDo・日課管理 ③ 状況対応型自発発話＋発話ガバナンス ④ カレンダー参照（読み取り専用）。横断原則（サブ役割分担・low/advanced 境界の再定義 §4.2.1）を含む。実装記録は docs/daily-support-design.md（§11.1 実装確定事項）と docs/release-notes/v0.2.0.md。

### 6.2 Tier A — 第 2 弾（v0.3 実装済み・2026-07-24）

**2026-07-18 のスコープ選定（v0.2.0 リリース後）**: ① 朝・夜の定例会話 ② 天気・生活情報 の 2 件を **§4.7 として要件化 → M11（天気基盤）/ M12（定例会話）で実装済み**（ブランチ `feat/v0.3-regular-talk`。実装記録は regular-talk-design.md）。音声入力は下表のとおり **v0.4 では不採用**。

| 項目 | 状態 |
|---|---|
| 朝・夜の定例会話 | **§4.7.1・M12 で実装済み**（v0.3） |
| 天気・生活情報 | **§4.7.2・M11 で実装済み**（v0.3。警報・気温差は非採用のまま将来課題へ） |
| 音声入力（プッシュ・トゥ・トーク） | **v0.4 では不採用**（2026-08-10 ユーザー裁定）。理由: **音声を使ったゴーストとのやり取りは完成度を極めて高くする必要があり、現時点では時期尚早**。技術的な実現性は確認済み（ローカル STT の候補は複数あり、Windows 標準の音声認識 API は MSIX 必須で本アプリからは使えないことも判明）だが、**体験の質を理由とした延期**であり、再検討時もその水準を満たせるかが判断基準になる。採用時は **§4.5.2「STT 廃止」決定の撤回 + §3.3 へのマイクチャネル追加**が必須。常時マイク監視は不採用、ホットキー/ボタンの明示起動のみ。ローカル認識前提 |

### 6.3 Tier B — 有用だが代替が強い（順位低）

| 項目 | 要点 |
|---|---|
| クイックメモ | 吹き出しから即登録の軽さに限定するなら価値。Windows 標準の付箋・Win+V と競合するため重いノート UI は作らない |
| クリップボード支援の拡張 | 履歴機能は Win+V と重複するため非採用。既存 📋 導線の advanced 変換（要約・翻訳・校正・整形）拡張に限定 |
| クイックコマンド | 事前登録ホワイトリスト方式のみ（AI に任意シェルコマンドを生成・実行させない）。音声入力とセットで初めて価値が出る |

### 6.4 Tier C — 後期（識別性を薄める / リスクが価値を上回る）

| 項目 | 要点 |
|---|---|
| 画面 OCR・画面理解 | PowerToys 等で代替可能。採用する場合は範囲選択・送信前プレビュー・クラウド送信明示・自動マスクを必須条件とする |
| ファイル検索・ランチャー | 前面に出すと「キャラクターアプリ」から「ランチャー」に寄りすぎる。補助機能に留める |
| ゴースト拡張基盤の高度化 | 権限マニフェスト・署名/ハッシュ検証・対応バージョン明示。第三者ゴースト流通が現実になってから。**外部ゴーストへの任意コード実行は許さない** |
| スマートホーム連携 | 構想のみ |
| **伺かゴースト（SHIORI 辞書）の変換** | **非目標（2026-08-31 裁定）。** 実装しない。技術的に不可能だからではなく（里々は宣言的で構造がほぼ 1:1 に対応する）、**得られるものと失うものが釣り合わないため**。① ugg の events 約 40 キーのうち伺かから供給源があるのは約 20 で、それは汎用マスコットの側。`focus_*` / `pomodoro_done` / `todo_*` / `reminder_*` / `situation_*` / `calendar_upcoming` / `regular_*` / `weather_*` と `system_messages` / `input_match` / `fallback` / `recall` / `monologue` は**供給源が存在せず**、ugg の存在理由にあたる側が結局手書きになる。② 辞書の `Line` は 1 応答 = 最大 2 発話だが、伺かのトークは話者が 4〜12 回入れ替わる脚本で、**その往復のリズム自体がキャラの個性**。スキーマの表現力差なので実装では改善しない。③ ゴーストは作者が書いた文章そのもので、条件分岐を潰す変換は「作者が設計したいつ何を言うか」を壊す。**著作権法 50 条により私的複製（30 条）は著作者人格権に及ばず**、同一性保持権の論点は手元利用でも消えない。界隈の許諾も作者ごとに真逆で機械判定できない。**例外は「原作者本人が自分のゴーストを移す」場合のみ**（他人のゴーストを変換する道具としては作らない） |
| **他人のキャラの人格記述の自動生成** | **レッドライン（2026-08-31）。** 第三者ゴーストの辞書から `persona` / `style_notes` を機械生成して LLM に演じさせる導線は作らない。原作者が一度も書いていない台詞を、そのキャラの名前で、原作者の訂正手段なしに生成し続ける装置になるため。**§4.2 の persona 配線を行う場合も、この線を先に確定させてから着手する** |

### 6.5 縮小・凍結（現行機能で優先度を下げる）

| 項目 | 方針 |
|---|---|
| 高品質 TTS（Irodori）の追加拡張 | 新音声モデル・演技機能の追加は凍結。バグ修正・安定化のみ |
| 台本読み上げの高度化 | 安定化を優先。大規模な編集機能・音声制作機能は後回し |
| ニュース機能の拡大 | 現状維持。本人に直接関係する情報（天気・予定・ToDo）を優先する |
| 汎用 Web 調査 | advanced 会話から明示的に要求された場合だけ起動する補助に留める |
| **伺か資産の変換ツール（`crates/nar2ugg/`）** | **凍結（2026-08-31 裁定）。シェル変換・ゴースト変換とも行わない。** scaffold（CLI・モジュール境界・出力契約は確定、変換ロジックは `todo!()`）のまま停止し、実装を進めない。ゴースト変換の不採用理由は §6.4。シェル変換は権利・技術の両面で成立するが、**ugg 本体の完成度を優先する判断**により着手しない。crate は削除せず、README 冒頭に凍結中である旨を明記して残す |

### 6.6 継続する技術的将来課題

| 項目 | 詳細 |
|---|---|
| 自動更新 | tauri-plugin-updater 採用検討。コード署名証明書取得が前提。 |
| マルチ言語 | 現状日本語のみ。UI とゴースト辞書の i18n 化。 |
| 多話者キャプション（高度） | 基本形は台本形式対応（script-reader-spec.md）で実装済み（caption は v3 本体では音声に反映されない。§6.0 の v0.5.7 への引き継ぎ）。残: 第 3 スロット以降・話者マッピング UI・VoiceDesign・cfg_scale_caption の行指定。 |
| マルチモニタ（残件） | 表示モニタの選択と DPI 違いのモニタへの移動は §4.1.6 へ要件化済み（§6.0）。**将来課題として残るのは、モニタごとに別々のキャラ X 位置を記憶すること**（現在はモニタ非依存の単一値で、移動時は新しいステージ幅に clamp する）。 |
| ゴースト DnD 配信 | §4.5.6 の検討項目の継続。 |
| OS 通知（Windows トースト） | §4.6.1 のフォールバックは現状アプリ内の帯で、ウインドウが見えていないと届かない（見えない間は未達として保留する）。本当に OS 通知へ落とすなら `tauri-plugin-notification` の採用と、通知権限・クリックでの復帰導線の設計が要る。 |
| カレンダー書き込み・双方向同期 | §4.6.4 は読み取り専用。OAuth 認証を伴う同期は要設計。 |
| 天気の高度化 | §4.7.2 は予報＋降雨の一言のみ。気象警報・注意報、気温差アラート、週間予報の読み上げ、複数地点は将来。 |
| クラウド同期 | 設定・記憶のデバイス間同期。要設計。 |
| アクセシビリティ | キーボード操作のフルカバー、スクリーンリーダー連携。 |

---

## 7. 参照

- [docs/daily-support-design.md](daily-support-design.md) — **日常支援 Tier S の Phase 2 設計書**（§4.6 の実装契約・DB・マイルストーン M7〜M10）
- [docs/regular-talk-design.md](regular-talk-design.md) — **定例会話と天気（v0.3）の Phase 2 設計書**（§4.7 の実装契約・天気 API 選定・マイルストーン M11〜M12）
- [docs/foundation-design.md](foundation-design.md) — **基盤・完成度（v0.4）の Phase 2 設計書**（§4.1.6 表示モニタ選択 / §4.4.4 advanced 独り言 / §4.4.6 時事ネタの実装契約・M13〜M15）
- [docs/text-reader-spec.md](text-reader-spec.md) — **テキスト読み上げツールの変更仕様書**（§4.5.8 の実装契約。v0.1.1 で出荷）
- [docs/script-reader-spec.md](script-reader-spec.md) — **テキスト読み上げ 台本形式対応の変更仕様書**（v0.1.2 で出荷。サンプルは `docs/samples/script-sample.md`）
- [docs/_legacy-v003/baseline-v0.0.3.md](_legacy-v003/baseline-v0.0.3.md) — v0.0.3 機能網羅スナップショット（**仕様検討のインプット**。v0.0.3 の生 docs は 2026-07-24 の docs 整理で削除、原本はプロトタイプリポジトリと git 履歴に現存）
- [CLAUDE.md](../CLAUDE.md) — プロジェクトメタ情報
- ghosts/default/ — 既存ゴースト資産（Phase 2 で schema v3 へ移行）
- shells/default/ — 既存シェル資産

---

## 8. 改訂履歴

| 日付 | 版 | 内容 |
|---|---|---|
| 2026-06-18 | v1 | Phase 1 機能取捨選択 31 項目を反映、初版 |
| 2026-07-12 | v1.1 | 新機能提案の評価（有用性軸）を反映: §4.6 日常支援（Tier S: 統合リマインダー / ToDo・日課 / 状況発話＋発話ガバナンス / カレンダー参照）を新設、§4.2.1 に low/advanced 境界の再定義と AI 非依存の不変条件を追加、§4.5.3 に移行予告、§6 を Tier 別ロードマップへ再構成（音声入力を将来課題から Tier A へ移動・縮小/凍結方針を明文化） |
| 2026-07-18 | v1.2 | v0.2.0 リリース（§4.6 Tier S 全 4 機能実装済み）を受けた v0.3 スコープ確定（ユーザー裁定）: §4.7「定例会話と天気・生活情報」を新設（朝・夜の定例会話 = 設定時刻以降の初回アクティブ時に配達・§4.6.2 朝告知を吸収 / 天気 = キー不要 API・地域手動設定・既定オフ・降雨の一言）、§6.2 を確定状態に更新（音声入力は v0.4 以降へ）。reviewer 反証を反映: 朝夜を独立 2 カテゴリ化・🔕 は 3 回で当該枠 OFF（間隔バックオフ非適用）・夜間静音優先と UI 警告・アクティブ基準の固定・降雨の一言と朝告知の吸収規則・座標の市区町村丸め・API 退路と出典表示・§3.3 に天気/カレンダーのチャネル追記・§4.6/§6.1 を実装済み表記へ |
| 2026-07-23 | v1.2.1 | §4.7.1 発火規則に当日失効（設定時刻から一定時間、既定 6 時間）を追記。Phase 2 設計（regular-talk-design.md v1）の反証レビューで確定した「夜の『おはよう』・朝夜連続配達」防止の裁定を要件側へ反映 |
| 2026-07-24 | v1.2.2 | §4.7（定例会話・天気）を **M11（天気基盤）/ M12（定例会話）で実装完了**（ブランチ `feat/v0.3-regular-talk`）。§4.7 見出し・§6.2 Tier A を「実装済み」表記へ更新。要件本文の変更はなし（実装状態のみ反映）。 |
| 2026-07-24 | v1.2.3 | §7 参照の整理（docs 整理に追随）: 削除した docs/README.md・_legacy-v003/spec.md へのリンクを除去し baseline 行に注記。要件本文の変更はなし。 |
| 2026-08-10 | v1.3 | **v0.4 スコープ確定（ユーザー裁定）= 基盤・完成度**。§6.0「現在のスコープ」を新設し 4 項目を明記（Tier 別の履歴表 §6.1 以降とは別建て）。**§4.1.6 に「表示モニタの選択」を要件化**: 明示選択が 1 秒監視より優先／見つからなければ主モニタへ退避しつつ選択は保持／**戻る保証は「表示構成が同じなら」に限定**（OS のモニタ識別子は接続順で入れ替わりうるため）／`monitor_pref` キーに永続化／DPI 違いのモニタへの移動／キャラ X 位置は新しいステージ幅へ clamp。§4.1.1 の移動範囲を整合（**キャラのドラッグによるモニタ跨ぎは引き続き不可**）。§6.6 のマルチモニタ行は残件（モニタごとの X 位置記憶）のみに縮小。**§4.4.4 に advanced 独り言のキャッシュ補充方針**（ストックは再起動をまたぐ／補充時に時事ネタを織り込む／ストックに生成時刻を持たせる／コストと降格時フォールバック／**§4.5.5 履歴クリアの対象**）を追記。**§4.4.6 に時事ネタの賞味期限 1 週間を追記** — 織り込み時と**発話時の二段で失効判定**し、発話時に 7 日超なら辞書の独り言へ差し替える。**§6.2 の音声入力を「v0.4 では不採用」に更新し、理由（音声でのやり取りは完成度を極める必要があり時期尚早）と技術調査の結論を記録**。§4.4.4/§4.4.6 は要件の追加ではなく、既存要件（未達だった分）の明確化。 |
| 2026-08-31 | v1.4 | **伺か資産の変換を非目標として確定（ユーザー裁定）**。§6.4 に「ゴースト（SHIORI 辞書）の変換」を非目標として追加し、判断理由 3 点（イベント収率の非対称／`Line` が 2 発話固定で往復のリズムを運べない／著作権法 50 条により私的複製が著作者人格権に及ばないこと）を記録。あわせて「他人のキャラの人格記述の自動生成」をレッドラインとして明記。§6.5 に変換ツール `crates/nar2ugg/` の凍結を追加（**シェル変換・ゴースト変換とも行わない**）。要件本文（§1〜§5）の変更はなし。 |
| 2026-09-02 | v1.5 | **v0.5 スコープ確定（ユーザー裁定）= 「宣言どおりに動く」**。§6.0 を v0.4 から差し替え、5 項目（persona/prompt の配線 / 月額上限を本当に効かせる / 記憶 UI / 配達の可視性判定 / 再発を止める土台）を明記。新機能はゼロ。**§4.2.7 の月額上限について「次月リセットで復帰。」は既に定めがあり、v0.4.1 で見送った際の理由（spec に記述が無く仕様判断が要る）は誤りだった**ことを記録。あわせて v0.5 で扱わないもの（recall / question_curiosity / 口パク / DB 退避）と、**実ユーザーが開発者本人のみのため配布まわりを扱わない**ことを明記。要件本文（§1〜§5）の変更はなし。 |
| 2026-09-02 | v1.5.1 | **§4.2.2b「キャラクターの人格記述（persona）」を新設**（v0.5 項目 1 の実装に伴う要件化）。`ghost.json` の `characters.*.persona` / `prompt.max_chars_per_line` / `prompt.style_notes` を advanced の 3 経路（チャット / 独り言 / 定例会話の言い換え）の system prompt に載せることを明記。省略時は既定文へフォールバック。**人格記述の生成は行わない**（§6.4 のレッドラインを参照）。 |
| 2026-09-02 | v1.5.2 | **§4.2.7 に上限判定の要件を追記**（v0.5 項目 2 の実装に伴う）。判定を LLM 呼び出し前の単一ゲートとし 3 経路すべてが通ること、降格タイマーで解除しないこと、告知済みを当月タグで永続化すること、**当月利用状況の設定画面表示（`get_cost_status`）**と**料金表未掲載モデルの警告**（ローカル LLM は 0 が正しいのでリモートのときのみ）を明記。 |
| 2026-09-02 | v1.5.3 | **§4.6.1 の「Windows トースト」を実態（アプリ内トースト）に訂正**（v0.5 項目 4）。OS 通知は実装範囲外であることを明記し、§6.6 へ将来課題として移した。あわせて**ウインドウ不可視時は未達として保留する**要件、60 秒再試行と 2 件以上の集約、可視性判定を `can_deliver` に足さないこと（二重ゲート禁止）を明記。 |
| 2026-09-02 | v1.5.4 | **§5 にログファイルの仕様を明記**（v0.5 項目 5）。`%APPDATA%\ugg\ugg.log`、2MB で 1 世代退避。従来 `eprintln!` は dev のコンソール専用でリリース版では失われていた。 |
| 2026-09-02 | v1.5.5 | **docs 整理**（tidy-docs、v0.5.0 タグ後）: §7 参照に `foundation-design.md` を追加（v0.4 の Phase 2 設計書が正本から 1 度も参照されていなかった）。要件本文の変更はなし。 |
| 2026-09-02 | v1.6 | **v0.5.1 スコープ**（棚卸しで見つかった残り 7 件の乖離の解消）に着手。§4.5.6 に**ゴースト切替時のシェル追従**（`default_shell` を読む。ユーザーの明示選択を優先）を追記。 |
| 2026-09-02 | v1.6.1 | **§4.5.5 にエクスポートの対象と DB 整合性検査を追記**（v0.5.1）。export をキャッシュ 3 つを除く全テーブルへ（schema `ugg-export-v2`）。起動時 `quick_check` は**検知と保全のみで DB を作り直さない**方針を明記し、`get_db_health` コマンドを追加。 |
| 2026-09-02 | v1.6.2 | **§4.1.4 に口パクの実装契約を追記**（v0.5.1）。開口フレームは `shell.json` に書かせず自動検出し、boot payload では `talk_poses` として`poses` と分けて送る（`poses` は LLM に渡る pose 語彙そのもののため）。駆動は本発話のみ。 |
| 2026-09-02 | v1.6.3 | **§4.2.4 に問いかけパターンの実装契約を追記**（v0.5.1）。**advanced の掛け合いパターン 5** として実装（構造はパターン1 と同じ、内容が問いかけで終わる、確率 5%）。architecture が「辞書の events キー + probability」と書いて spec と矛盾していたため、spec を正本として architecture 側を訂正した。 |
| 2026-09-04 | v1.6.4 | **v0.5.1 完了**（棚卸しで残した 7 件の乖離をすべて解消）。§4.5.1 自動再生ポリシー / §4.5.6 `default_shell` 追従 / §4.5.5 export 全テーブル化と DB 整合性検査 / §4.1.4 口パク / §4.2.4 問いかけパターン、および `recall` の実装（契約は architecture §6.2）。要件の新設はなく、いずれも**既存の宣言に実装を一致させたもの**。 |
| 2026-09-05 | v1.6.5 | **v0.5.1 のリリース前監査を受けた是正**。① **§6.0 を v0.5 から v0.5.1 へ差し替え** — 正本が「v0.5 に入れないもの」として `recall` / 問いかけ / 口パク / DB 退避を挙げたままで、**v0.5.1 で出荷した 4 件を非スコープと宣言している状態**だった。② **§4.5.1 に `additionalBrowserArgs` の置換挙動を明記** — `additionalBrowserArgs` は wry の既定引数を置換するため、自動再生ポリシーは既定で既に効いており、自前の値だけを書くと `--disable-features=msWebOOUI,msPdfOOUI,msSmartScreenProtection` を失う。実装もそれに合わせて是正した。③ §4.5.5 の保全に「破損 1 件につき 1 回・WAL も運ぶ」を追記。④ §4.2.6 の想起トリガーは**ユーザー入力語のみ**から作ると明記（アプリの定型文を渡すと「ユーザー」「希望」「興味」が低負荷モードの応答を奪う）。 |
| 2026-09-05 | v1.6.6 | **v0.5.1 の実機確認で見つかった 2 件を受けた明確化**（v0.5.2）。① §4.5.5 の「起動も止めない」に**マイグレーション失敗の扱い**を明記 — 整合性検査を非致命にしても直後の `migrate()` を致命扱いにすれば起動しない。**v0.5.1 は実際にここで panic して破損 DB では起動できず、本書が 4 箇所で宣言していた挙動が成立していなかった。** ② 救出コピーの作成に失敗したときは残骸（0 バイトファイル）を残さないことを明記 — 残すと「保全は 1 回だけ」のガードが失敗を成功として固定する。 |
| 2026-09-05 | v1.6.7 | **§6.0 の配布まわりを実態に合わせる**。本節は「外部配布を始める時点で改めてスコープに載せる」と書いていたが、**v0.5.2 のインストーラを public リポジトリの Release へ公開した時点で、ユーザー裁定により「現時点ではスコープに載せない」と決定**した。書いてある条件を満たしたのに載せないので、**未署名・更新導線なしを既知のまま出荷している**ことを明記する（宣言と実態の乖離を残さないため）。要件本文（§1〜§5）の変更はなし。 |
| 2026-09-05 | v1.6.8 | **docs 整理（tidy-docs、v0.5.2 タグ後）**。削除対象はゼロ（docs 12 本すべてが不可侵リスト該当かつコード被参照あり）。① **§7 参照に `text-reader-spec.md` / `script-reader-spec.md` を追加** — この 2 本は正本から 1 度も参照されていなかった（前回の整理で `foundation-design.md` に見つけたのと同型）。② **本改訂履歴の並びを版の昇順に整列** — v1.6.5〜v1.6.7 を降順に挿し込んでいたほか、v1.2 / v1.2.1 が末尾に取り残されていた。要件本文の変更はなし。 |
| 2026-09-06 | v1.7 | **v0.5.3 スコープ確定（ユーザー裁定）=「つないでも動く」**。§6.0 を v0.5.1 から差し替え、9 項目を明記。第三者レビュー（Codex）の指摘 14 件を実コードで検証して**全件妥当**と判定し、見送り無しで全件を対象にした（判定は `docs/codex-review-2026-09.md`。v0.5.3 のリリースノートへ消化して削除済み）。残っていたのは単独機能ではなく**つなぎ目**（旧版からの更新 / 保存後の再保存 / 連続通知 / 非同期の中断）で、**単機能テストでは 14 件のうち 1 件も捕まらなかった**。**うち 2 件は v0.5.1 でこちらが誤って棄却・誤って説明したもの**。裁定 3 件: 履歴は**直近 8 往復を上限に注入**（チャット経路のみ）/ 通知は**キュー化して前を消さない**（§4.1.3 の改訂を伴う）/ コスト集計が失敗したら**止めて告知する**（§4.2.7）。要件本文（§1〜§5）の改訂は実装時に行う。 |
| 2026-09-08 | v1.7.1 | **v0.5.3 項目 1（復旧導線）の実装に伴う §4.5.5 の改訂**。① エクスポートを**部分救出**に（schema `ugg-export-v2` → `v3`、`failed_tables` を追加）。**破損したらまず export で控えるよう案内している当の機能が、1 テーブルの読み取り失敗でJSON を一切出さずに終了していた。** ② 整合性検査を `journal_mode` / `foreign_keys` より**前**に移す（ヘッダ破損では pragma の時点で失敗し、v0.5.2 の「起動も止めない」に到達しなかった。`journal_mode` の変更は DB を書き換えるため、退避前に原本へ書いてもいた）。③ 既存の退避・救出コピーを**名前だけで採用しない**（救出コピーは `quick_check` まで確認。v0.5.1 が残した 0 バイトの残骸を「救出済み」と誤認していた）。 |
| 2026-09-10 | v1.7.2 | **v0.5.3 項目 2（更新でデータを失わない）の実装に伴う §4.5.6 の改訂**。DnD 導入を**非破壊**と明記: 別ディレクトリで展開・検証してから差し替え、旧版は待避してから入れ替え、戻せなければ待避先を残す。作業ディレクトリと待避先は `ghosts/` `shells/` の外。**確認と展開で同じ manifest を選ぶ**ことも明記（v0.5.2 までは「最初に一致」と「最も浅い」で食い違いえた）。 |
| 2026-09-10 | v1.7.3 | **v0.5.3 項目 3（保存したあとの再保存で巻き戻らない）の実装に伴う §4.5.6・§4.6.4 の改訂**。① `default_shell` の**追従結果を設定フォームへ戻す**ことを明記（保存後の書き戻しがゴースト・シェルの select だけ対象外で、パネルを開いたままの再保存が追従を黙って取り消していた）。② カレンダーの**ソース構成更新を原子的に**行うと明記（設定の保存とキャッシュ全消去は両方成立するか、どちらも起きないか）。 |
| 2026-09-10 | v1.7.4 | **v0.5.3 項目 5（課金の保護を異常時にも効かせる）に伴う §4.2.7 の改訂**。当月コストを**集計できない間は LLM を呼ばない**（fail-closed）と明記。上限 0（無制限）は対象外。**黙って low に落ちず、上限超過とは別の告知**（辞書 `cost_unknown`）を出す。この告知済みフラグだけは**永続化しない**（記録先の `app_settings` も同時に失敗しうるため、プロセスごとに 1 回）。 |
| 2026-09-10 | v1.7.5 | **v0.5.3 項目 8（非同期の中断を跨いで確認する）に伴う §4.5.2 の追記**。TTS の共通再生制御に「**中断は非同期処理を跨いで効かせる**（待ちを含む処理のあとに毎回世代を確認し、失効していたら音も口パクも始めない）」を明記。 |
| 2026-09-10 | v1.7.6 | **v0.5.3 項目 7（記憶とログの後始末）に伴う §4.2.6・§5 の改訂**。① 自動記憶のトリガー語も**ユーザー入力から採る**と明記（v0.5.1 で塞いだのはオンボーディングの定型文だけで、LLM 要約文を使う自動抽出経路に同じ穴が残っていた。一致しなければトリガーを作らない＝**誤爆より不発**）。② **診断ログに会話本文を残さない**と明記（ログは履歴クリアの対象外で、載せると消す手段が無い）。 |
| 2026-09-10 | v1.7.7 | **v0.5.3 項目 4（通知を後続の発話で消さない）に伴う §4.1.3 の改訂**。「新発話で interrupt」の**例外として通知を明記**。通知はキューに入れて割り込まず、描画中に割り込まれたらキューの先頭へ戻す（消すと配達済みの記録だけが残り永久に届かない）。対象は system_message と配達の Notice のみで、Ambient（状況発話・独り言・定例会話）は含めない。入力促し・メニュー前口上は使用中扱いにしない（滞留を避けるため）。 |
| 2026-09-10 | v1.7.8 | **v0.5.3 項目 6（問いかけを会話として閉じる）に伴う §4.2.4 の改訂**。問いかけの応答を成立させるために**直近の会話を注入する**と明記（v0.5.2 まで履歴注入は常に空で、「次のターンで自然に受ける」は成立していなかった）。**直近 8 往復 + 合計文字数の上限**（超えたら古い方から）、**ユーザー発言の直前のキャラ発話は残す**（問いかけ本体）、掛け合いは名前つきで 1 応答にまとめる、**チャット経路のみ**（独り言補充には渡さない）。 |
| 2026-09-10 | v1.7.9 | **v0.5.3 のリリース前監査を受けた是正**。① §4.2.4: 履歴注入の対象を **advanced の会話行だけ**に限定すると明記（チャットログには独り言・通知・状況発話も入り、そのまま送ると §3.3 / §4.6.4 の送信物の範囲を超える。往復が 1 つも無ければ何も注入しない）。② §5: 会話本文をログに残さない対象は **LLM を叩く経路すべて**（最初の実装はチャットの応答だけを直し、同じ形の独り言バッチが漏れていた）。HTTP エラーボディは消さず**長さで頭打ち**にする。 |
| 2026-09-11 | v1.7.10 | **docs 整理（tidy-docs、v0.5.3 タグ・push 後）**: §6.0 が出荷済みのスコープを「現在のスコープ」として据え置いていたので、**出荷済みである旨と次スコープで差し替える旨**を冒頭に明記（v0.5.1 で踏んだ「§6.0 が実態とずれたまま残る」の再発防止）。あわせて **v0.5.3 を Release へ公開していない**ことと、その結果 Releases の最新（v0.5.2）とタグの最新が食い違うことを記録。**要件本文の変更はなし。** |
| 2026-09-11 | v1.8 | **v0.5.4 スコープ確定（ユーザー裁定）=「声を新しくする」**。§6.0 を v0.5.3 から差し替え。**Irodori-TTS の合成モデルを v3 → v4.1-Small へ**。調査で判明した前提を §6.0 に記録: ① **ugg はすでに v4.1 世代のコードを固定している**（`IRODORI_TTS_ZIPBALL` の pin が upstream の「Update default model to v4.1-Small」commit そのもの）ため、取り残されているのはモデル ID だけ ② 推論経路が `transformers` から使うのは `AutoTokenizer` のみで、upstream の `>=5.12.1` は `--no-deps` により一度も効いていない ③ トークナイザは実行時に HF から取る（v3 も同様）。**v4.1-Anime は不採用**（学習データの出所が不明で、base 自身の倫理条項「声優の声を同意なくクローンしない」との関係を検証できない。§6.4 / Live2D / 伺かゴースト変換と同じ基準）。量子化版も不採用。着手順は **0（spike）を先に決着させてから 1〜4**。 |
| 2026-09-11 | v1.8.1 | **v0.5.4 のスコープを分割（ユーザー裁定）=「更新が届く」**。着手前の spike が前提を 2 つとも崩したため、§6.0 をモデル差し替えから**ランタイムの更新経路の修正**へ差し替えた。① `assets_ready` が**パッケージの存在しか見ておらず**、2026-08-15 に上げた pin が既存ユーザーへ一度も届いていない（実機の `direct_url.json` は pin 前の `refs/heads/main.zip` を記録）② **v4.1-Small は transformers 5 を要求する**（`text_encoder_type: "pretrained"` → `transformers.initialization`。v3 はこの経路を通らないので現状は動く）。連鎖して huggingface_hub 0.27 → 1.x も必要。**モデル差し替えと依存の major 移行は v0.5.5 へ**。v4.1-Anime 不採用の裁定は据え置き。 |
| 2026-09-11 | v1.8.2 | **v0.5.4 スコープへの反証レビュー結果を反映**。① 実装上の制約 3 件を明記（**固定した版がそのまま入るとは限らない**＝`huggingface_hub==0.27.0` 指定に対し実機 0.36.2 / **共有 HF キャッシュを掃除の対象にしない**＝他アプリの資産が同居 / 途中で切れた DL を「取得済み」と数えない）。② **v4.1 の漢字読み改善は ugg には効かない**と訂正（OpenJTalk でかな化してから渡すため。CER の出典も `600M-v3-VoiceDesign` で ugg が使う `500M-v3` の値ではない）。③ **未計上の契約⇔実装の乖離を 1 件計上**: `caption` は v3 本体が `use_caption_condition: false` のため**一度も効いていない**のに、契約表と script-reader-spec は使えると宣言している。v0.5.5 の差し替えで閉じるが、**有効化されること自体を検証対象**にする。④ 採用モデルの倫理条項の扱いを v0.5.5 の論点として記載。⑤ spike のコードはリポジトリへ入れない旨を明記。 |
| 2026-09-11 | v1.8.3 | **§6.0 項目 2 の訂正（実装前に追って気づいた）**。「`assets_ready` を存在から一致へ／一致しないものを導入済みと呼ばない」と書いていたが、そのまま実装すると**動いている環境を壊す**。フロントは `canUseReal = gpuOk && assetsOk` で判定し、false のとき `tts_irodori_use_real_model` を黙って false へ倒して永続化するため、古いが動いているランタイムを「未導入」に落とすと**ユーザーの設定が勝手に消える**。項目 2 を「**使える**」と「**最新**」を分ける形へ改め、`assets_ready` の意味は変えないことにした。 |
| 2026-09-11 | v1.8.4 | **§6.0 実装上の制約に 1 件追加（実機検証の直前に発見）**。項目 3 は「`python` は入れ直せないので伝えて止まる」と定めたが、実装は**記録が無いことを「python の版が違う」と読み替えて**いた。記録が無い環境では対象を `current_pins()` から丸ごと組み立てており、そこに `python` が混ざるため更新経路が冒頭で Err になり、**v0.5.4 が対象にしている環境がちょうど 1 つも更新できない**状態だった（しかも「Python 本体の版が変わっています。全部消して入れ直してください」という事実でない案内が出る）。版は `python.exe --version` で**実物に聞ける**ので、判定を実物へ移し、聞けなかったときは「古い」と言わないことにした。 |
| 2026-09-11 | v1.8.5 | **実機検証（E-7）の 1 回目で判明した 2 件**。① **更新の成否判定が誤っていた** — 「`irodori_tts` / `dacvae` / `silentcipher` が全部 import できること」を絶対条件にしていたが、実機の `silentcipher` は `pydub` が無く**一度も import できたことが無い**（upstream が意図的に任意依存にしており、合成は透かし無しで成立する）。そのため**既存環境では必ずロールバックし、更新が誰にも一度も成功しない**状態だった。判定を**入れ直し前後の差分**へ改め、悪化したものだけを失敗とみなすことにした（項目 3 の約束は「動いていた環境を壊さない」であって「壊れていた環境を直す」ではない）。② 付随して **SilentCipher の透かしが一度も効いていない**ことが判明したので、v0.5.5 の引き継ぎに計上した。 |
| 2026-09-11 | v1.8.6 | **E-7 の実機検証中に見つけた 2 件を v0.5.5 の引き継ぎへ計上**（どちらも v0.5.4 のスコープ外）。① **サイドカーの stderr が `[hf-download]` 以外すべて捨てられる**ため、異常終了の理由がユーザーにもログにも残らない。今回は VRAM 不足が原因だったが、更新が壊したのかを外部ツール無しに判別できなかった。② **実モデルの ON/OFF が起動中のサイドカーに反映されない**（`ensure_sidecar_running` が `mock` を見ずに既存ポートを返す）。切り替えたのにビープ音が鳴る形で現れる。 |
| 2026-09-11 | v1.8.7 | **リリース前監査（release-audit）の指摘 8 件のうち 6 件をタグ前に修正**。① **初回 DL と更新が排他でなかった** — 記録が無い環境では両方のボタンが押せ、同じ `site-packages` を 2 経路が同時に触ると退避も復元も守れない（項目 3 の約束の前提が崩れる）。② **前回「守るために残した」退避を、次の実行が冒頭で無条件に消していた** — そこにあるのは唯一残った旧版。まず戻し、戻せなければ場所を伝えて止まる形へ。③ **退避が途中まで進んで失敗すると戻していなかった**（ディレクトリは動いたが dist-info で失敗する経路）。あわせて UI が一律「元の状態のままです」と事実でないことを言っていたのを修正。④ **初回 DL 経路が、入れていない `python` まで最新として記録していた** — 更新経路だけ直して隣に同じ型を残していた。記録の書き方を 1 本（`record_after_install`）に寄せた。⑤ **更新しても稼働中のサイドカーは旧コードのまま**だったので、更新前に停止する。⑥ 改訂履歴の昇順・型の doc コメント位置。**見送り 1 件**: `run_python` の同期待ちが tokio ワーカーを塞ぐ件（既存の DL 経路と同じ形で、この回の退行ではない）。**新たに計上**: モデル本体は更新の対象外（`current_pins()` は 4 URL のみ）で、v0.5.5 でモデル ID を変えても既存環境には届かない。 |
| 2026-09-11 | v1.8.8 | **v0.5.4 インストール版の実機確認で見つけた 1 件を v0.5.5 の引き継ぎへ計上**（スコープ外・既存の不具合）。**強制終了するとサイドカーが孤児化し、GPU を掴んだまま残る。** 終了フックが走らないため `irodori.shutdown()` が呼ばれず、残ったサイドカーはアイドル監視も効かない（監視側が死んでいる）。1 つで数 GB の VRAM を占めるので落とすたびに積み上がり、**音声が途切れ途切れになる**形で現れる。`ready.json` に `pid` を記録しているのに起動時の掃除経路が無い。直し方は pid を撃つのではなく、記録されたポートへ shutdown を投げてから `ready.json` を消す。 |
| 2026-09-11 | v1.8.9 | **v0.5.4 タグ後の docs 整理（tidy-docs）**。削除ゼロ・参照切れゼロ（docs は 12 ファイルすべて不可侵リスト。`codex-review-2026-09.md` と `_legacy-v003/spec.md` は改訂履歴が「削除済み」と記録している行、`parameters.md` は upstream Irodori-TTS 側のファイル）。**失効 1 件を是正**: §6.0 が「これから作る」書き方のままで出荷済みの印が無く、次のスコープ決定時に未着手と読めた。出荷済みであることと、**「入れないもの」は出荷後も有効**（v0.5.1 の前例）であることを冒頭に明記した。 |
| 2026-09-11 | v1.9.0 | **孤児サイドカーの扱いを v0.5.5 の優先項目へ格上げ**。実測で、advanced モードのローカル LLM（LM Studio の `llama-server`）が**常駐で 7.7 GB** を占めており、16 GB のうち Irodori が使えるのは**約 8 GB**しかないと判明した。サイドカー 1 つで数 GB なので、**孤児が 1 つ残っただけで次の合成が入らない**。2026-09-11 に実際そうなり「音声が途切れ途切れ」として現れた。**advanced（LLM）と Irodori 実モデルが同じ VRAM を取り合う**前提を v0.5.5 の設計時に明示的に置くこと。掃除だけでなく、**足りないときに何が起きているかを伝える**経路も要る（現状は「HTTP 通信に失敗しました」だけ ＝ stderr 破棄と同じ穴）。 |
| 2026-09-11 | v1.9.1 | **§6.0 の事実誤認を訂正**（v0.5.5 スコープの反証レビューで発覚）。v1.8.1 で「`PretrainedTextBackbone` が `transformers.initialization` を**無条件 import** する」と書いたが、upstream の固定 SHA（`irodori_tts/model.py:822`）で確認したところ **`__init__` の中、しかも try/except の中**だった。結論（v4.1-Small は transformers 5 を要求する）は変わらないが、**「import ゲートで捕まる」という誤解を招く**記述だった。実際には `import irodori_tts` は通り、破綻は初回合成時に起きて無言で VOICEVOX へ落ちる。依存の版を上げる回では**「import できる」ではなく「1 回合成できる」を成否の条件にする**旨を明記した。 |
| 2026-09-11 | v1.10 | **v0.5.5 スコープ確定（ユーザー裁定）=「環境ぜんぶが更新の対象になる」**。§6.0 を v0.5.4 から差し替え。当初は「モデル差し替え」だったが、**反証レビューが前提を崩した**ため v0.5.4 と同じ形で分割した（差し替えは v0.5.6）。① **更新判定は固定 URL 4 本しか見ていない** — `COMMON_REQUIREMENTS` / `IRODORI_EXTRA_REQUIREMENTS` の版を変えても `outdated` は空で、**transformers 4→5 を届ける手段が 1 本も無い**。`resolved` は記録しているのに判定に未使用。**対象外なのはモデルだけではなかった。** ② **モデル ID は毎起動で上書きされる `sidecar.py` にある**ため、変えた瞬間コードだけ新しくなって重みが無い＝「届かない」ではなく**静かに壊れる**（無言で VOICEVOX へ落ちる）。③ **import ゲートは依存の版ずれを原理的に検出できない**（`transformers.initialization` の import は `__init__` の try/except の中）。項目: ①落ちた理由が残る（**stderr と合成エラーの reason の 2 経路とも**。後者は辞書キーの存在で捨てている）②孤児サイドカーの掃除（**pid もポートも撃たず応答の形で識別**。`ready.json` の単一スロットも直す）③更新の対象を広げる（名前付き pip 要件の版差 + モデルの repo/revision。**旧モデルを消さない**）④更新中の発話を止める（`ensure_sidecar_running` が busy を見ていない）。**成否の条件を「import できる」から「1 回合成できる」へ**改める。 |
| 2026-09-14 | v1.10.1 | **Irodori 高速化の実測と裁定を §6.0「v0.5.6 へ引き継ぐ調査結果」へ記録**（ユーザー裁定: **高速化は v0.5.6 で行う**。v3 の linear 16・sway 8 とも**音は許容**）。記事「irodori-TTS を速くする方法を理解したい」（株式会社Rosso公式note、2026-09-04）を起点に、この機材でランタイム組み込みの設定と `Aratako/Irodori-TTS-v4.1-Small-MF` を実測した。**効くのはステップ数だけで、bf16 は速くならない**（VRAM が減るだけ）。**MF（4 ステップ）は v3 現行の約 3.5〜4 倍速だが、v3 の sway 8 とほぼ同じ速さ**で、利点は少ステップでの品質側にある。記事の CUDA Graph + バケット化は、ランタイム内部への差し込みが大きく差し替えで作り直しになるため、差し替え後に再判断する。**v0.5.5 のスコープは変えない。** |
| 2026-09-14 | v1.10.2 | **MF の音質裁定を §6.0「v0.5.6 へ引き継ぐ調査結果」へ追記**。ユーザーが MF の fp32 と bf16 を聴き比べ、**bf16 で「十分許容範囲」**と裁定した。v0.5.6 の差し替え先を MF（bf16）にする判断材料として扱う（bf16 なら VRAM ピーク 3.1 GB で、v3・fp32 の 3.5 GB より少ない）。差し替え先の確定そのものは v0.5.6 のスコープ確定で行う。 |
| 2026-09-14 | v1.10.3 | **v0.5.5 のリリース前監査（release-audit）が指摘した §6.0 と実装の食い違いを訂正**（CLAUDE.md 規律 3）。① 項目 3 の「`resolved` を使う」を、ユーザー承認（2026-09-11）どおり**要件文字列そのものの比較**に書き直した。② 「1 回合成できる」を成否の条件にすることは **v0.5.5 では見送り**と明記し、「入れないもの」にも載せた（v0.5.6 で依存の major を上げる前に必ず入れる）。③ 同意の水準を具体化した（確認ダイアログに、数 GB になりうること・更新中は VOICEVOX で話すこと・戻せる範囲を書く）。④ 監査で見つかった基準値の欠陥に合わせ、**欄が空の記録は v0.5.4 の固定の基準値で読む**ことと、**モデルを取得した revision の置き場所から読む**ことを明記した。⑤ 直さなかったもの（二重起動ガード・Job Object・伏字の限界・停止の確認）を v0.5.6 への引き継ぎに記録した。 |
| 2026-09-14 | v1.10.4 | **v0.5.5 インストール版の実機確認で見つけた「サイドカーの stderr の日本語が化ける」を §6.0 の v0.5.6 への引き継ぎに記録**。実環境のサイドカーは Windows のエラー文を cp932 で出しており、v0.5.5 ではその行で stderr の読み取りが理由を残さず止まっていた（項目 1 の stderr 側が起動直後しか成り立っていなかった）。読み取りが止まる欠陥は v0.5.5 で直し、実機で効き目を確認した。**文字コードの食い違い自体は、手元の起動では再現せず理由が未解明**なので、推測の修正を入れずに引き継ぐ。契約・設定フィールド・イベントの変更なし。 |
| 2026-09-14 | v1.10.5 | **v0.5.5 タグ後の docs 整理（tidy-docs）**。削除ゼロ（docs の 27 ファイルはすべて不可侵リスト）。**失効 1 件を是正**: §6.0 に出荷済みの印が無く、次のスコープ決定時に未着手と読めた（v0.5.3・v0.5.4 のタグ後の整理と同じ形）。注記には「v0.5.5 に入れないもの」が出荷後も有効であることと、「v0.5.6 へ引き継ぐ調査結果」を差し替え後の §6.0 へ持ち越すことを書いた。要件本文の変更はなし。 |
| 2026-09-17 | v1.10.6 | **v0.5.6 前の docs 整理（外部レビューの検証）**。外部レビューの 19 件を実装と突き合わせてすべて妥当と判定し、同じ形の取り残しと合わせて是正した。本文では、辞書セクション名を v3 の実名へ（`recall` / `monologue` / `system_messages.update_available`。コスト・更新などのシステム告知の置き場所は `events` ではなく `system_messages`）、§4.3.5 のメニュー項目に「予定・ToDo」、§4.5.3 をリマインダー移行後の姿へ（v0.2 で §4.6.1 へ移行済み）、§4.5.8 の「120 トークン」の定義と caption が v3 本体で音声に反映されない注記、§3.1 の二段トーストが未実装である注記。§6.0 の v0.5.6 への引き継ぎに、終了のあいさつの経路差と二段トーストの扱い（どちらもユーザー裁定で「記録して v0.5.6 のスコープで判断」）を追加。**要件の変更はなし**（実装済みの事実と既に決まっていたことへの追随）。 |
| 2026-09-19 | v1.11 | **v0.5.6 スコープ確定（ユーザー裁定）=「速くして、乗り換えに備える」**。§6.0 を v0.5.5 から差し替え。当初の案はモデル差し替えまでの一括だったが、反証レビュー（別 Opus、2026-09-18）が**高速化は差し替え無しで届く**ことを spec 自身の実測から示した（v3 の sway 8 は、参照音声の事前変換まで入れると MF とほぼ同じ速さで、音は 09-14 に許容と裁定済み。更新なしで届く）ため、**v0.5.4・v0.5.5 と同じ形でもう一度分割した**（MF への乗り換えと依存の major 移行は v0.5.7）。反証と検証が見つけた土台の欠落（更新の途中失敗が成功済みの退避を戻さずに消す・読み先がビルドの定数だけで決まる・記録の欄が空の環境の読み先が未定義・合成を 1 回試す経路が無い・新版のインストーラが動いている ugg を強制終了する）を項目にした。**ユーザー裁定 7 件**: 分割して v3 を先に速くする／二重起動は台帳の所有者だけ（完全な single-instance は入れない）／終了のあいさつを揃える／§3.1 の二段トーストは取り下げ、代わりに告知の未達を扱う／参照音声の事前変換を項目 1 に含める／透かしの裁定は v0.5.7 へ（2 回目の先送り）／更新の錠をプロセスをまたぐものにする。v0.5.5 で直さなかったもの（二重起動・Job Object・停止の確認・cp932・終了のあいさつ・二段トースト）は項目 2〜6 と「入れないもの」に吸収した。伏字は、stderr にも通す側を項目 2 に入れ、白名簿化は裁定を取っていないので「見つけたが今回の裁定に含めていないもの」へ置いた。「入れないもの」は裁定と出どころを行ごとに書き、出荷後も有効な 3 件（v4.1-Anime 不採用・量子化版・v0.6 刷新）を再掲し、ハッシュ検証（v0.4.1 の見送りの継続）と CUDA Graph（09-14 の記録）を足した。**新設した節**: v0.5.7 へ引き継ぐもの（付随作業・裁定が要るもの・受け入れ条件・着手前の spike）と、見つけたが今回の裁定に含めていないもの。VRAM が足りないときに伝える経路は v0.5.7 へ移した（告知の追加を伴うため）。「引き継ぐ調査結果」は持ち越し、トークナイザと MF の箇条に取得経路の注記を足した。**実測表の訂正**: v3 の linear 16 と sway 8 の行は参照 latent を事前計算した条件で測っていたのに、その条件を書き落としていた。計測の記録と突き合わせて直し、MF の bf16 の行を足した。**書き込み後の 2 回のレビューで直した自分の誤り**: ① 事前変換ありの値を ugg のいまの経路の速さとして書いた ② cp932 の仮説が v0.5.5 の手元の再現と矛盾していた ③ 参照音声の事前変換を「メモリの中だけ」と説明したが、上流の公開 API はファイルのパスしか受け取らない（ファイルに置く形へ改めた）④ MF は実行時のステップ数と CFG を無視するのに「そのステップ数で回る」と書いた ⑤ caption の受け入れ条件が seed を固定しておらず必ず通る形だった ⑥ 3f の錠の範囲が更新だけで、同じ錠を見る初回導入と合成の側、台帳の錠が漏れていた（開発方針 7）。§3.1 の注記と、caption の引き継ぎ先（§4.5.8・§6.6）を直した。要件本文（§3.1・§4.4.2・§4.6.2 ほか）の改訂は各項目の実装時に行う。 |
| 2026-09-19 | v1.11.1 | **v0.5.6 項目 1（速くする）と項目 2 の文字コードの実装に伴う §6.0 の改訂**。① **v1.11 で「非公開のメソッドに頼らない」とした判断を覆した**: 上流には変換結果を作る公開 API が無いので、事前変換はランタイム自身が使う `_load_reference_latent` で作る。作れない・読めないときは参照 wav に戻る（遅くなるだけで喋れる）。変換結果を使った合成が失敗したら参照 wav でやり直し、それで合成できたときだけ変換結果を消してそのサイドカーの間は使わない。GPU のメモリ不足では止めない。事前変換のキーに**コーデック**と両者の精度・参照の前処理を入れた（値を作るのはコーデックで、合成モデルだけをキーにするとコーデックの更新で古い値を黙って読む — 実装のレビューで発覚）。② **cp932 の原因を観測で確定させた**: dev が起動したサイドカーの中で `stderr.encoding=cp932`・`isolated=1`。パイプへは ANSI コードページで書く CPython の既定が原因で、`._pth` の isolated のため環境変数では変えられない。`sidecar.py` の `reconfigure` で UTF-8 にし（`-X utf8` は使わない）、受け側の 3 か所は UTF-8 → Shift_JIS で読む。項目 2 に architecture の改訂先を足した。dev での確認（2026-09-19）で見つけた asyncio の接続リセットのログを「見つけたが今回の裁定に含めていないもの」に足した。 |
| 2026-09-20 | v1.11.2 | **v0.5.6 項目 2 の残り（進捗を行ごとに・無進捗の中断・stderr の伏字）の実装に伴う §6.0 の改訂**。実装で分かったこと 3 件を本文に反映した: ① **パイプ越しでは pip も huggingface_hub も取得中に進捗を出さない**（pip の rich は端末でないと描かず、hub の tqdm は `disable=None` の既定で端末でないと出ない）ので、行ごとに流すだけでは「画面が固まる」は解けない — pip は `--progress-bar raw`（24.1 以降）、hub はモデル取得の間だけ判定を差し替える ② **無進捗を出力だけで数えると、正常な処理を止める**（pip は torch の展開中、黙って書き続けるだけで CPU もほとんど使わない）ので、読み書きと CPU を Job Object の集計で見る ③ 「中断」は**固まったものを自動で止めること**として実装した（ユーザーが押すボタンは新しいコマンドになるため入れない）。あわせて **Job Object は 6 か所のうち 2 か所を項目 2 で先に入れた**ことを項目 4 に注記し、**錠を握ったまま上限なく待つ通信 1 か所**（同じ形の取り残し）に上限を付けたことを書いた。**レビューで見つかった自分の誤りを 1 つ訂正**: `ff68e6c` の掃討結果と architecture は「VOICEVOX のダウンローダの経路も日本語が読める」と書いていたが、**色付けの制御文字を落とす処理が 1 バイトずつ文字に積み直しており、日本語は化けたままだった**（v0.5.6 で直した）。**契約・設定フィールド・DB スキーマの変更なし。** |
| 2026-09-23 | v1.11.3 | **v0.5.6 項目 3（更新を 1 つのトランザクションにする）の実装に伴う §6.0 の改訂**。項目 3 の末尾に「実装した形」を足した（a〜f から変えた・足したところ）。**実装順を a → d → c → b → e → f に変えた**（b の不合格は「全部戻す」で終わるので、d より先に作ると戻せないのに「戻す」と言う期間ができる）ことを本文に書き戻した。設計案の反証は 4 観点のうち 1 観点だけが完走し（残りは週の利用制限で落ちた）、指摘 14 件を全件採った。重いものは 4 件: **GPU は差分で見る**（見えなければ飛ばすだけだと、更新が CUDA を壊しても成功として記録が進む）／**ゲートの合否を絶対値で決めない**（戻した状態でもう一度試して切り分ける）／**版の控えは全配布の走査**（記録用の関数は約 24 件しか見ない）／**生きているサイドカーがいる間は始めない**（設計案の「site-packages は掴まない」は誤り）。ユーザー裁定 2 件（2026-09-20）: ゲートの材料はいまある参照音声（無ければ確かめずに成立）／GPU が見えない環境は確かめずに成立。**コマンド・イベント・設定フィールド・DB スキーマの変更なし**（`sidecar.py` に起動引数 `--synth-once` / `--voice-ref` を足した）。 |
| 2026-09-24 | v1.11.4 | **v0.5.6 項目 4（孤児と二重起動）の実装に伴う §6.0 の改訂**。項目 4 の末尾に「実装した形」を足した: 持ち主の判定（この ugg / 生きているほかの ugg / 持ち主なし）、1 件ずつ読む・差し替えで書く、台帳の錠（足すときは錠が取れなくても書き、消すときは見送る）、ugg の寿命に結びつける Job（サイドカーと zip の展開。子を起動する本番のコードは項目 3b のゲートで 7 か所になっていた）、実モデルの ON/OFF（錠の判定を既存のポートより先に）、導入・更新の入口で持ち主のいない孤児を止める。**コマンド・イベント・設定フィールド・DB スキーマの変更なし。** |
| 2026-09-24 | v1.11.5 | **v0.5.6 項目 5（終了のあいさつを揃える）の実装に伴う改訂**。§4.4.2 を「トレイと右クリックメニューの「終了」は同じ経路。見えていれば終了前の確認かあいさつをしてから、隠している・最小化しているときはすぐ終了。待っている間にもう一度押せばすぐ終了」へ。§4.3.5 の「終了」に同じ経路であることを書いた。§4.6.2 の「終了前の確認」は発話ガバナンスを通さない（操作への応答。静音中も出す）と明記し、ガバナンスに従うのは起動時の確認だけにした。**ユーザー裁定（2026-09-24）: 隠していればすぐ終了し、聞こえていたあいさつが無くなってもよい。** §6.0 項目 5 に「実装した形」。**コマンド・イベント・設定フィールド・DB スキーマの変更なし**（`quit_app` の引数と戻り値は変えていない）。 |
| 2026-09-24 | v1.11.6 | **v0.5.6 項目 6（告知は「届いた」と確かめてから済みにする）の実装に伴う改訂**。§3.1 から「重大なものはトーストで二段表示」を取り下げ（2026-09-19 裁定）、見えていない間は出さないこと・1 回だけ出す告知は届いたときにだけ済みにすることを書いた。§4.6.1 の「§3.1 の二段構え」を「§3.1 のフォールバック」へ（取り下げの対象ではない、発話できないときのトースト）。§6.0 項目 6 に「実装した形」。**コマンド・イベント・設定フィールド・DB スキーマの変更なし。** |
